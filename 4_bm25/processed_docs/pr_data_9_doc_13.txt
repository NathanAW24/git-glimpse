Pull Request Number: 3274
Title: refactor(docs): overall documentation
Base Branch: main
Head Branch: docs/eng-567
Author: wingkwong
URL: https://github.com/nextui-org/nextui/pull/3274
State: MERGED
Created At: 2024-06-18T09:22:59Z
Merged At: 2024-06-22T14:06:26Z
Participants: wingkwong, jrgarciadev, TIMMLOPK

Description:
Closes #2599
📝 Description

Add a brief description

⛳️ Current behavior (updates)

Please describe the current behavior that you are modifying

🚀 New behavior

Please describe the behavior or changes this PR adds

💣 Is this a breaking change (Yes/No):

📝 Additional Information

Summary by CodeRabbit

Documentation

Updated warning messages and removed redundant spaces across various component documentation files for clarity.
Added instructions for using pnpm and monorepo architecture in the Tailwind CSS setup.
Updated dependency versions and installation instructions in framework-specific documentation (Astro, Next.js, Remix, Vite, etc.).
Provided additional notes for hoisting dependencies and setting up Tailwind CSS with NextUI v2.0.

Commits:
- fix(docs): update framer motion version in docs\n- refactor(docs): hoisted dependencies setup\n- refactor(docs): add bun command\n- refactor(docs): revise tailwindcss setup\n- refactor(docs): add missing highlight lines and content in nextjs page\n- fix(docs): incorrect highlighted lines in input\n- refactor(docs): remove unnecessary space\n- refactor(docs): add color and revise content\n- refactor(docs): revise indentation in nextjs page\n

Labels:
📋 Scope : Docs

Comments:
- linear: <p><a href="https://linear.app/nextui-inc/issue/ENG-567/[feature-request]-improving-installation-instructions-in-the-guide">ENG-567 [Feature Request] Improving installation instructions in the guide</a></p>\n- vercel: [vc]: #rgA275NITPjrgL36hxd3eeiVXEOEdaOEIuP0T0toh48=:
**The latest updates on your projects**. Learn more about [Vercel for Git ↗︎](https://vercel.link/github-learn-more)

| Name | Status | Preview | Comments | Updated (UTC) |
| :--- | :----- | :------ | :------- | :------ |
| **nextui-docs-v2** | ✅ Ready ([Inspect](https://vercel.com/nextui-org/nextui-docs-v2/6tYdBbcGmn6JCMSgtkbxoGtmg7Cf)) | [Visit Preview](https://vercel.live/open-feedback/nextui-docs-v2-git-docs-eng-567-nextui-org.vercel.app?via=pr-comment-visit-preview-link&passThrough=1) | 💬 [**Add feedback**](https://vercel.live/open-feedback/nextui-docs-v2-git-docs-eng-567-nextui-org.vercel.app?via=pr-comment-feedback-link) | Jun 20, 2024 5:31am |
| **nextui-storybook-v2** | ✅ Ready ([Inspect](https://vercel.com/nextui-org/nextui-storybook-v2/FLHkurcrPmtFKv6AJCCGSQBs5eYz)) | [Visit Preview](https://vercel.live/open-feedback/nextui-storybook-v2-git-docs-eng-567-nextui-org.vercel.app?via=pr-comment-visit-preview-link&passThrough=1) | 💬 [**Add feedback**](https://vercel.live/open-feedback/nextui-storybook-v2-git-docs-eng-567-nextui-org.vercel.app?via=pr-comment-feedback-link) | Jun 20, 2024 5:31am |


\n- changeset-bot: ###  ⚠️  No Changeset found

Latest commit: 351d53de3a0ce07f7ce49f897f108f5444a40bca

Merging this PR will not cause a version bump for any packages. If these changes should not result in a new version, you're good to go. **If these changes should result in a version bump, you need to add a changeset.**

<details><summary>This PR includes no changesets</summary>

  When changesets are added to this PR, you'll see the packages that this PR includes changesets for and the associated semver types

</details>

[Click here to learn what changesets are, and how to add one](https://github.com/changesets/changesets/blob/main/docs/adding-a-changeset.md).

[Click here if you're a maintainer who wants to add a changeset to this PR](https://github.com/nextui-org/nextui/new/docs/eng-567?filename=.changeset/tricky-planes-dream.md&value=---%0A%22%40nextui-org%2Fdocs%22%3A%20patch%0A---%0A%0Arefactor(docs)%3A%20overall%20documentation%0A)

\n- coderabbitai: <!-- This is an auto-generated comment: summarize by coderabbit.ai -->
<!-- walkthrough_start -->

## Walkthrough

The changes primarily focus on improving clarity and consistency in the NextUI documentation, particularly around installation instructions and setup for different environments and package managers. Key updates include refining instructions for pnpm usage, updating dependencies (e.g., Framer Motion from version 4 to 11), and adjusting Tailwind CSS setup steps. The goal is to provide a more streamlined, error-free setup experience, especially for pnpm and monorepo users, thus addressing several common pain points in the guide.

## Changes

| Files                                                              | Changes Summary                                                                                                                                                 |
|--------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `.../docs/components/package-managers.tsx`                         | Updated a warning message in a blockquote to clarify command usage for individual installations.                                                                                      |
| `.../docs/content/blog/nextui-v2.mdx`, `.../docs/customization/theme.mdx`, `.../frameworks/...`                | Added notes for using pnpm and monorepo architecture, updating to Framer Motion 11, hoisted dependencies setup for pnpm users, Tailwind CSS setup adjustments.                                                                                         |
| `.../docs/components/accordion.mdx`, `.../docs/components/avatar.mdx`, `.../docs/components/...`                | Removed unnecessary `{" "}` characters and redundant spaces across various components for consistency.                      |
| `.../docs/content/docs/components/input.mdx`                        | Updated highlighted lines in `CodeDemo` components for better visual representation.                                                                            |
| `.../docs/guide/installation.mdx`, `.../docs/guide/upgrade-to-v2.mdx` | Enhanced installation instructions, emphasizing pnpm setup and providing additional notes for package managers and Tailwind CSS.                                    |

## Sequence Diagram(s)

Not applicable due to the nature of the changes being primarily documentation-related with no significant control flow modifications.

## Assessment against linked issues

| Objective                                                                                           | Addressed | Explanation                                                                                                         |
|-----------------------------------------------------------------------------------------------------|-----------|----------------------------------------------------------------------------------------------------------------------|
| Emphasize pnpm setup and provide clear instructions in the installation guide for pnpm users (#2599) | ✅         |                                                                                                                      |
| Update dependencies to newer versions and mention setting up `.npmrc` for Vercel deployment (#2599) | ✅         |                                                                                                                      |
| Clarify Tailwind CSS setup and handling specific issues when `next-themes` package is added (#2599)  | ✅         |                                                                                                                      |



<!-- walkthrough_end --><!-- This is an auto-generated comment: raw summary by coderabbit.ai -->

<!--

# Existing changesets

apps/docs/components/docs/components/package-managers.tsx: ### Summary

The change involves updating a warning message within a blockquote component in a React component file to clarify that the command provided is for individual installation only and can be skipped if a specific package is already installed globally.

### Alterations to the declarations of exported or public entities

No alterations were made to the declarations of exported or public entities in this diff.

---

apps/docs/content/blog/nextui-v2.mdx: ## Short Summary

The change involves adding a note about using pnpm and monorepo architecture to point to the root `node_modules` in the `tailwind.config.js` file for NextUI v2.0.

## Alterations to the declarations of exported or public entities

- `js {8,13-14}` in `apps/docs/content/blog/nextui-v2.mdx` → `js {9,14-15}` in `apps/docs/content/blog/nextui-v2.mdx`

---

apps/docs/content/docs/components/accordion.mdx: ### Summary

The diff in `accordion.mdx` involves removing unnecessary `{" "}` characters after `<Spacer y={4} />` in various sections of the file.

### Alterations to the declarations of exported or public entities

No alterations to the declarations of exported or public entities were identified in this diff.

---

apps/docs/content/docs/components/avatar.mdx: ### Summary

The diff in `avatar.mdx` involves removing redundant `{" "}` characters after `<Spacer y={4} />`.

### Alterations to the declarations of exported or public entities

No alterations to the declarations of exported or public entities.

---

apps/docs/content/docs/components/checkbox.mdx: ### Summary

The change involves a minor adjustment in spacing within the code snippet related to checkbox customization in the `checkbox.mdx` file.

### Alterations to the declarations of exported or public entities

No alterations to the declarations of exported or public entities were identified in this diff.

---

apps/docs/content/docs/components/chip.mdx: ## Short Summary

In the provided diff for `chip.mdx`, the change involves removing a redundant space character after the `<Spacer y={4} />` element.

## Alterations to the declarations of exported or public entities

No alterations were made to the declarations of exported or public entities in this diff.

---

apps/docs/content/docs/components/circular-progress.mdx: ### Summary

The diff for `circular-progress.mdx` involves removing redundant `<Spacer y={4} />` lines within the `CircularProgress` component customization sections.

### Alterations to the declarations of exported or public entities

No alterations to the declarations of exported or public entities were identified in the diff.

---

apps/docs/content/docs/components/code.mdx: ## Short Summary

The change involves removing a space character in the JSX code related to a spacer component.

## Alterations to the Declarations of Exported or Public Entities

- `<Spacer y={4} />{" "}` in `apps/docs/content/docs/components/code.mdx` → `<Spacer y={4} />` in `apps/docs/content/docs/components/code.mdx`

---

apps/docs/content/docs/components/date-input.mdx: ## Short Summary
The change involves removing a space character in the JSX code related to a `Spacer` component in the `date-input.mdx` file.

## Alterations to the declarations of exported or public entities
No alterations were made to the declarations of exported or public entities in this diff.

---

apps/docs/content/docs/components/date-picker.mdx: ### Summary

In the `date-picker.mdx` file, the changes focus on removing trailing spaces after `<Spacer y={4} />` in various sections like **Data Attributes**, **Accessibility**, and **API**.

### Alterations to the declarations of exported or public entities

No alterations to the declarations of exported or public entities were identified in the `date-picker.mdx` file.

---

apps/docs/content/docs/components/input.mdx: ### Summary

The changes in `input.mdx` involve updating the line numbers in the highlighted sections of `CodeDemo` components for different features like Password Input, Clear Button, Custom Styles, and Custom Implementation. The adjustments mainly affect the visual representation and positioning of these components within the documentation.

### Alterations to the declarations of exported or public entities

- `<CodeDemo title="Password Input" highlightedLines="25" files={inputContent.password} />`
- `<CodeDemo title="Clear Button" highlightedLines="7" files={inputContent.clearButton} />`
- `<CodeDemo title="Custom Styles" highlightedLines="13-31" files={inputContent.customStyles} />`

---

apps/docs/content/docs/components/link.mdx: ### Summary

The diff in `link.mdx` involves removing redundant spaces (`<Spacer y={4} />{" "}`) in multiple sections of the file.

### Alterations to the declarations of exported or public entities

No alterations to the declarations of exported or public entities were made in this diff.

---

apps/docs/content/docs/components/radio-group.mdx: ### Summary

The diff in `radio-group.mdx` involves removing redundant spaces (`<Spacer y={4} />{" "}`) in the file.

### Alterations to the declarations of exported or public entities

No alterations to the declarations of exported or public entities were identified in this diff.

---

apps/docs/content/docs/components/snippet.mdx: ## Short Summary

The change involves removing a space character in JSX code related to customizing icons for copy and check functionalities.

## Alterations to the declarations of exported or public entities

No alterations to the declarations of exported or public entities were identified in the provided diff.

---

apps/docs/content/docs/components/spinner.mdx: ## Short Summary
The change involves removing a space character in the JSX code related to a Spinner component, specifically in the `Spacer` component.

## Alterations to the declarations of exported or public entities
- `Spacer y={4} /` in `apps/docs/content/docs/components/spinner.mdx` → `Spacer y={4}/` in `apps/docs/content/docs/components/spinner.mdx`

---

apps/docs/content/docs/components/switch.mdx: ### Summary

The diff for `switch.mdx` involves removing redundant spaces in the file, specifically in the `<Spacer y={4} />` elements, making the formatting more consistent.

### Alterations to the declarations of exported or public entities

No alterations to the declarations of exported or public entities were identified in the provided diff.

---

apps/docs/content/docs/components/textarea.mdx: ### Summary

The diff for `textarea.mdx` involves minor changes in spacing within the file, specifically removing unnecessary spaces around `<Spacer y={4} />`.

---

apps/docs/content/docs/customization/theme.mdx: ### Summary
The diff in `theme.mdx` involves adding a note about using pnpm and monorepo architecture and updating line numbers in a code block related to pointing to the ROOT `node_modules` in the `tailwind.config.js` file.

### Alterations to the declarations of exported or public entities
- `js {8,13-14}` → `js {9,14-15}` in `theme.mdx`

---

apps/docs/content/docs/frameworks/astro.mdx: ## Short Summary

The diff in `astro.mdx` involves updating dependencies like Framer Motion to version 11, adding a new package manager command for installing NextUI, introducing a hoisted dependencies setup for pnpm users, and providing additional notes for using Tailwind CSS with NextUI.

## Alterations to the Declarations of Exported or Public Entities

- `Framer Motion 4` → `Framer Motion 11` in the requirements section
- Added `bun: "bun add @nextui-org/react framer-motion"` to the installation command list for NextUI
- Added hoisted dependencies setup instructions for pnpm users
- Modified the `tailwind.config.cjs` file to ensure it points to the root `node_modules` for pnpm and monorepo architecture users
- Added a note about using `client:visible` for NextUI components to work properly on the client side

---

apps/docs/content/docs/frameworks/nextjs.mdx: ### Summary

The diff for `apps/docs/content/docs/frameworks/nextjs.mdx` primarily involves updating the version requirement for Framer Motion from version 4 to version 11. Additionally, it includes instructions for hoisted dependencies setup for pnpm users, modifications in Tailwind CSS setup instructions, and minor code snippet adjustments for clarity.

### Alterations to the declarations of exported or public entities

- `Framer Motion 4` → `Framer Motion 11` in the requirements section
- Added instructions for hoisted dependencies setup for pnpm users
- Adjusted code snippets in the Tailwind CSS setup section for pointing to the ROOT `node_modules`
- Code snippet adjustments in the `app/providers.tsx` and `app/layout.tsx` files for better clarity
- Modified instructions in the `pages/_app.js` file for wrapping the Component with the `NextUIProvider`

---

apps/docs/content/docs/frameworks/remix.mdx: ### Summary

The diff in `remix.mdx` includes changes related to version requirements for dependencies like Framer Motion, addition of a new installation command for NextUI, setup instructions for hoisted dependencies when using pnpm, and guidance on Tailwind CSS setup, particularly for pnpm and monorepo architecture.

### Alterations to the declarations of exported or public entities

- `Framer Motion 4` → `Framer Motion 11` in the requirements section
- Added `bun: "bun add @nextui-org/react framer-motion"` to the installation commands for NextUI
- Added instructions for hoisting dependencies when using pnpm in a `.npmrc` file
- Modified the `tailwind.config.js` code snippet to ensure it points to the ROOT `node_modules` when using pnpm and monorepo architecture

---

apps/docs/content/docs/frameworks/vite.mdx: ### Summary

The diff in `vite.mdx` includes updates to dependency versions, addition of a new command for installing NextUI, instructions for hoisting dependencies when using pnpm, and a note for pnpm and monorepo users about pointing to the root `node_modules` when configuring Tailwind CSS.

### Alterations to the declarations of exported or public entities

- `Framer Motion 4` in the requirements section changed to `Framer Motion 11`
- Added a new installation command for NextUI: `bun add @nextui-org/react framer-motion`
- Added instructions for hoisting dependencies for pnpm users in the `.npmrc` file
- Added a note for pnpm and monorepo users to point to the root `node_modules` in the Tailwind CSS setup

---

apps/docs/content/docs/guide/installation.mdx: ### Summary

The diff in `apps/docs/content/docs/guide/installation.mdx` primarily involves updating version requirements for dependencies like Framer Motion and providing additional instructions for hoisting dependencies when using pnpm. It also includes clarifications and adjustments in the installation steps for NextUI and Tailwind CSS.

### Alterations to the declarations of exported or public entities

- `Framer Motion 4` → `Framer Motion 11` in the requirements section
- Added instructions for hoisting dependencies when using pnpm
- Clarified steps for global and individual installations of NextUI
- Updated Tailwind CSS setup instructions for pnpm users

---

apps/docs/content/docs/guide/upgrade-to-v2.mdx: ### Summary

The diff in `upgrade-to-v2.mdx` involves updating dependencies like Framer Motion to version 11, modifying setup instructions for hoisting dependencies using pnpm, and adjusting Tailwind CSS setup in NextUI v2. Additionally, it includes guidance on importing components and setting up Tailwind CSS plugins for NextUI.

### Alterations to the declarations of exported or public entities

- `Framer Motion 4` in the requirements list updated to `Framer Motion 11`
- Added hoisted dependencies setup instructions for pnpm users
- Tailwind CSS setup code in `tailwind.config.js` modified to include a reference to the ROOT `node_modules`
- Instructions regarding importing components from individual packages instead of `@nextui-org/react`

# New changesets

apps/docs/content/docs/frameworks/nextjs.mdx: ### Summary

The diff for `apps/docs/content/docs/frameworks/nextjs.mdx` primarily involves updating the version requirement for Framer Motion from version 4 to version 11. Additionally, it includes instructions for hoisted dependencies setup for pnpm users, modifications in Tailwind CSS setup instructions, and minor code snippet adjustments for clarity.

### Alterations to the declarations of exported or public entities

- `Framer Motion 4` → `Framer Motion 11` in the requirements section
- Added instructions for hoisted dependencies setup for pnpm users
- Adjusted code snippets in the Tailwind CSS setup section for pointing to the ROOT `node_modules`
- Code snippet adjustments in the `app/providers.tsx` and `app/layout.tsx` files for better clarity
- Modified instructions in the `pages/_app.js` file for wrapping the Component with the `NextUIProvider`

-->

<!-- end of auto-generated comment: raw summary by coderabbit.ai --><!-- This is an auto-generated comment: pr objectives by coderabbit.ai -->

<!--

### PR Summary

**Title:** refactor(docs): overall documentation  
**User:** wingkwong  
**Number:** 3274  

**Description:**  
This pull request aims to refactor the overall documentation of the project. It addresses the issues outlined in issue #2599 by improving the installation instructions in the guide, especially for users who encountered problems using a React application created with Vite. The changes include clarifying the setup steps for pnpm, providing detailed instructions for deployment on platforms like Vercel, and addressing issues related to the `next-themes` package and Tailwind CSS configuration.

**Closes:** #2599

### Objectives from Linked Issues

**Issue #2599: [Feature Request] Improving installation instructions in the guide**

**Description:**  
The issue highlights several problems users face with the current installation instructions for the UI library, particularly when using React applications created with Vite. The main objectives are:

1. **Clarify Setup for pnpm:** The current guide's section on setting up pnpm is not clear, causing users to overlook it and face issues. The request is to emphasize the importance of this step earlier in the guide.

2. **Deployment on Vercel:** Users encountered missing styles when deploying on Vercel. The solution involves setting up `.npmrc` on Vercel, with a reference to Vercel's official instructions for using private dependencies.

3. **`next-themes` Package Issue:** After installing the `next-themes` package, users experienced a significant reduction in CSS file content. The issue was resolved by modifying the `tailwind.config.js` file, ensuring the styles remained intact.

The proposed solution includes detailed steps and code snippets to address each of these issues, enhancing the overall clarity and usability of the installation guide.

-->

<!-- end of auto-generated comment: pr objectives by coderabbit.ai --><!-- This is an auto-generated comment: shorter summary by coderabbit.ai -->

<!--

### AI-generated Summary of Generated Summaries

The changes across multiple files in the documentation and component directories primarily focus on the following key areas:

1. **Version Updates and Dependency Management**:
   - The version requirement for Framer Motion has been updated from version 4 to version 11 in multiple framework-specific documentation files (`astro.mdx`, `nextjs.mdx`, `remix.mdx`, `vite.mdx`, `installation.mdx`, `upgrade-to-v2.mdx`).
   - New installation commands for NextUI have been added, including support for the `bun` package manager.
   - Instructions for hoisting dependencies when using pnpm have been introduced, including modifications to `.npmrc` files and ensuring `tailwind.config.js` points to the root `node_modules` for pnpm and monorepo architectures.

2. **Code and Configuration Adjustments**:
   - Minor code snippet adjustments for clarity and accuracy have been made in various documentation files (`nextjs.mdx`, `astro.mdx`, `remix.mdx`, `vite.mdx`, `installation.mdx`, `upgrade-to-v2.mdx`).
   - Updates to Tailwind CSS setup instructions to accommodate pnpm and monorepo users, ensuring proper configuration in `tailwind.config.js`.

3. **Formatting and Redundant Code Removal**:
   - Redundant space characters (`{" "}`) and unnecessary `<Spacer y={4} />` elements have been removed from multiple component documentation files (`accordion.mdx`, `avatar.mdx`, `checkbox.mdx`, `chip.mdx`, `circular-progress.mdx`, `code.mdx`, `date-input.mdx`, `date-picker.mdx`, `link.mdx`, `radio-group.mdx`, `snippet.mdx`, `spinner.mdx`, `switch.mdx`, `textarea.mdx`).
   - Adjustments to highlighted lines in `CodeDemo` components for better visual representation and positioning within the documentation (`input.mdx`).

4. **Specific Documentation Enhancements**:
   - Addition of notes and clarifications about using pnpm and monorepo architectures in `tailwind.config.js` for NextUI v2.0 (`nextui-v2.mdx`, `theme.mdx`).
   - Clarified steps for global and individual installations of NextUI, and guidance on importing components from individual packages instead of `@nextui-org/react`.

### Alterations to the Declarations of Exported or Public Entities

The following list details the changes in the signatures of entities before and after the modifications for each file:

1. **apps/docs/content/blog/nextui-v2.mdx**:
   - Before: `js {8,13-14}`
   - After: `js {9,14-15}`

2. **apps/docs/content/docs/components/code.mdx**:
   - Before: `<Spacer y={4} />{" "} `
   - After: `<Spacer y={4} />`

3. **apps/docs/content/docs/components/spinner.mdx**:
   - Before: `Spacer y={4} /`
   - After: `Spacer y={4}/`

4. **apps/docs/content/docs/customization/theme.mdx**:
   - Before: `js {8,13-14}`
   - After: `js {9,14-15}`

5. **apps/docs/content/docs/frameworks/astro.mdx**:
   - Before: `Framer Motion 4`
   - After: `Framer Motion 11`
   - Added: `bun: "bun add @nextui-org/react framer-motion"`
   - Added: Hoisted dependencies setup instructions for pnpm users.
   - Modified: `tailwind.config.cjs` to point to root `node_modules`.
   - Added: Note about using `client:visible` for NextUI components.

6. **apps/docs/content/docs/frameworks/nextjs.mdx**:
   - Before: `Framer Motion 4`
   - After: `Framer Motion 11`
   - Added: Instructions for hoisted dependencies setup for pnpm users.
   - Adjusted: Code snippets in the Tailwind CSS setup section for root `node_modules`.
   - Adjusted: Code snippets in `app/providers.tsx` and `app/layout.tsx` for clarity.
   - Modified: Instructions in `pages/_app.js` for wrapping the Component with `NextUIProvider`.

7. **apps/docs/content/docs/frameworks/remix.mdx**:
   - Before: `Framer Motion 4`
   - After: `Framer Motion 11`
   - Added: `bun: "bun add @nextui-org/react framer-motion"`
   - Added: Hoisted dependencies setup instructions for pnpm users.
   - Modified: `tailwind.config.js` to point to root `node_modules`.

8. **apps/docs/content/docs/frameworks/vite.mdx**:
   - Before: `Framer Motion 4`
   - After: `Framer Motion 11`
   - Added: `bun add @nextui-org/react framer-motion`
   - Added: Hoisted dependencies setup instructions for pnpm users.
   - Added: Note for pnpm and monorepo users to point to root `node_modules` in Tailwind CSS setup.

9. **apps/docs/content/docs/guide/installation.mdx**:
   - Before: `Framer Motion 4`
   - After: `Framer Motion 11`
   - Added: Instructions for hoisting dependencies when using pnpm.
   - Clarified: Steps for global and individual installations of NextUI.
   - Updated: Tailwind CSS setup instructions for pnpm users.

10. **apps/docs/content/docs/guide/upgrade-to-v2.mdx**:
    - Before: `Framer Motion 4`
    - After: `Framer Motion 11`
    - Added: Hoisted dependencies setup instructions for pnpm users.
    - Modified: `tailwind.config.js` to include reference to root `node_modules`.
    - Added: Instructions regarding importing components from individual packages instead of `@nextui-org/react`.

-->

<!-- end of auto-generated comment: shorter summary by coderabbit.ai --><!-- commit_ids_reviewed_start -->

<!-- 75c5f8976aa83e69b9462f812f03033fe0eec5aa -->
<!-- 351d53de3a0ce07f7ce49f897f108f5444a40bca -->

<!-- commit_ids_reviewed_end --><!-- tips_start -->

---



Thank you for using CodeRabbit. We offer it for free to the OSS community and would appreciate your support in helping us grow. If you find it useful, would you consider giving us a shout-out on your favorite social media?

<details>
<summary>Share</summary>

- [X](https://twitter.com/intent/tweet?text=I%20just%20used%20%40coderabbitai%20for%20my%20code%20review%2C%20and%20it%27s%20fantastic%21%20It%27s%20free%20for%20OSS%20and%20offers%20a%20free%20trial%20for%20the%20proprietary%20code.%20Check%20it%20out%3A&url=https%3A//coderabbit.ai)
- [Mastodon](https://mastodon.social/share?text=I%20just%20used%20%40coderabbitai%20for%20my%20code%20review%2C%20and%20it%27s%20fantastic%21%20It%27s%20free%20for%20OSS%20and%20offers%20a%20free%20trial%20for%20the%20proprietary%20code.%20Check%20it%20out%3A%20https%3A%2F%2Fcoderabbit.ai)
- [Reddit](https://www.reddit.com/submit?title=Great%20tool%20for%20code%20review%20-%20CodeRabbit&text=I%20just%20used%20CodeRabbit%20for%20my%20code%20review%2C%20and%20it%27s%20fantastic%21%20It%27s%20free%20for%20OSS%20and%20offers%20a%20free%20trial%20for%20proprietary%20code.%20Check%20it%20out%3A%20https%3A//coderabbit.ai)
- [LinkedIn](https://www.linkedin.com/sharing/share-offsite/?url=https%3A%2F%2Fcoderabbit.ai&mini=true&title=Great%20tool%20for%20code%20review%20-%20CodeRabbit&summary=I%20just%20used%20CodeRabbit%20for%20my%20code%20review%2C%20and%20it%27s%20fantastic%21%20It%27s%20free%20for%20OSS%20and%20offers%20a%20free%20trial%20for%20proprietary%20code)

</details>

<details>
<summary>Tips</summary>

### Chat

There are 3 ways to chat with [CodeRabbit](https://coderabbit.ai):

- Review comments: Directly reply to a review comment made by CodeRabbit. Example:
	- `I pushed a fix in commit <commit_id>.`
	- `Generate unit testing code for this file.`
	- `Open a follow-up GitHub issue for this discussion.`
- Files and specific lines of code (under the "Files changed" tab): Tag `@coderabbitai` in a new review comment at the desired location with your query. Examples:
	- `@coderabbitai generate unit testing code for this file.`
	-	`@coderabbitai modularize this function.`
- PR comments: Tag `@coderabbitai` in a new PR comment to ask questions about the PR branch. For the best results, please provide a very specific query, as very limited context is provided in this mode. Examples:
	- `@coderabbitai generate interesting stats about this repository and render them as a table.`
	- `@coderabbitai show all the console.log statements in this repository.`
	- `@coderabbitai read src/utils.ts and generate unit testing code.`
	- `@coderabbitai read the files in the src/scheduler package and generate a class diagram using mermaid and a README in the markdown format.`
	- `@coderabbitai help me debug CodeRabbit configuration file.`

Note: Be mindful of the bot's finite context window. It's strongly recommended to break down tasks such as reading entire modules into smaller chunks. For a focused discussion, use review comments to chat about specific files and their changes, instead of using the PR comments.

### CodeRabbit Commands (invoked as PR comments)

- `@coderabbitai pause` to pause the reviews on a PR.
- `@coderabbitai resume` to resume the paused reviews.
- `@coderabbitai review` to trigger an incremental review. This is useful when automatic reviews are disabled for the repository.
- `@coderabbitai full review` to do a full review from scratch and review all the files again.
- `@coderabbitai summary` to regenerate the summary of the PR.
- `@coderabbitai resolve` resolve all the CodeRabbit review comments.
- `@coderabbitai configuration` to show the current CodeRabbit configuration for the repository.
- `@coderabbitai help` to get help.


Additionally, you can add `@coderabbitai ignore` anywhere in the PR description to prevent this PR from being reviewed.

### CodeRabbit Configration File (`.coderabbit.yaml`)

- You can programmatically configure CodeRabbit by adding a `.coderabbit.yaml` file to the root of your repository.
- Please see the [configuration documentation](https://docs.coderabbit.ai/guides/configure-coderabbit) for more information.
- If your editor has YAML language server enabled, you can add the path at the top of this file to enable auto-completion and validation: `# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json`

### Documentation and Community

- Visit our [Documentation](https://coderabbit.ai/docs) for detailed information on how to use CodeRabbit.
- Join our [Discord Community](https://discord.com/invite/GsXnASn26c) to get help, request features, and share feedback.
- Follow us on [X/Twitter](https://twitter.com/coderabbitai) for updates and announcements.

</details>

<!-- tips_end -->\n

Files Changed:
- apps/docs/components/docs/components/package-managers.tsx (modified, 6 changes)\n  Patch: @@ -97,9 +97,9 @@ export const PackageManagers = ({
         })}
       </Tabs>
       {showGlobalInstallWarning && (
-        <Blockquote className="my-2">
-          No need to install this package if <Code>@nextui-org/react</Code> is already installed
-          globally.
+        <Blockquote className="my-2" color="warning">
+          The above command is for individual installation only. You may skip this step if{" "}
+          <Code>@nextui-org/react</Code> is already installed globally.
         </Blockquote>
       )}
     </>\n- apps/docs/content/blog/nextui-v2.mdx (modified, 7 changes)\n  Patch: @@ -60,14 +60,19 @@ into React Server Components issues.
 NextUI v2.0 comes with a TailwindCSS plugin that enables the customization and addition of default themes.
 This plugin allows you to customize colors and layouts tokens that are used by NextUI components.
 
-```js {8,13-14}
+<Blockquote color="primary">
+**Note**: If you are using pnpm and monorepo architecture, please make sure you are pointing to the ROOT `node_modules`
+</Blockquote>
+
+```js {9,14-15}
 // tailwind.config.js
 const {nextui} = require("@nextui-org/react");
 
 /** @type {import('tailwindcss').Config} */
 module.exports = {
   content: [
     // ...
+    // make sure it's pointing to the ROOT node_module
     "./node_modules/@nextui-org/theme/dist/**/*.{js,ts,jsx,tsx}",
   ],
   theme: {\n- apps/docs/content/docs/components/accordion.mdx (modified, 6 changes)\n  Patch: @@ -151,7 +151,7 @@ Here's an example of how to customize the accordion styles:
 
 <CodeDemo title="Custom Accordion Styles" files={accordionContent.customStyles} />
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Data Attributes
 
@@ -172,7 +172,7 @@ Here's an example of how to customize the accordion styles:
 - **data-pressed**:
   When the accordion item is pressed. Based on [usePress](https://react-spectrum.adobe.com/react-aria/usePress.html).
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Accessibility
 
@@ -182,7 +182,7 @@ Here's an example of how to customize the accordion styles:
 - `aria-disabled` attribute for the accordion item.
 - `aria-controls` attribute for the accordion item.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## API
 \n- apps/docs/content/docs/components/avatar.mdx (modified, 4 changes)\n  Patch: @@ -150,7 +150,7 @@ You can customize any part of the avatar by using the `classNames` prop, each `s
 
 <CodeDemo title="Custom Avatar" files={avatarContent.custom} />
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Data Attributes
 
@@ -163,7 +163,7 @@ You can customize any part of the avatar by using the `classNames` prop, each `s
 - **data-focus-visible**:
   When the avatar is being focused with the keyboard. Based on [useFocusRing](https://react-spectrum.adobe.com/react-aria/useFocusRing.html), it is applied when `isFocusable` is `true` or when the `as` property is assigned as `button`.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## API
 \n- apps/docs/content/docs/components/checkbox.mdx (modified, 2 changes)\n  Patch: @@ -127,7 +127,7 @@ In case you need to customize the checkbox even further, you can use the `useChe
 - **data-loading**:
   When the checkbox is loading. Based on `isLoading` prop.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Accessibility
 \n- apps/docs/content/docs/components/chip.mdx (modified, 2 changes)\n  Patch: @@ -93,7 +93,7 @@ You can customize the `Chip` component by passing custom Tailwind CSS classes to
 
 <CodeDemo title="Custom Styles" files={chipContent.customStyles} />
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## API
 \n- apps/docs/content/docs/components/circular-progress.mdx (modified, 6 changes)\n  Patch: @@ -92,7 +92,7 @@ You can customize the `CircularProgress` component by passing custom Tailwind CS
   highlightedLines="9-12"
 />
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Data Attributes
 
@@ -103,7 +103,7 @@ You can customize the `CircularProgress` component by passing custom Tailwind CS
 - **data-disabled**:
   Indicates whether the progress is disabled. Based on `isDisabled` prop.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Accessibility
 
@@ -113,7 +113,7 @@ You can customize the `CircularProgress` component by passing custom Tailwind CS
 - Determinate and indeterminate progress support.
 - Exposes the `aria-valuenow`, `aria-valuemin`, `aria-valuemax` and `aria-valuetext` attributes.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## API
 \n- apps/docs/content/docs/components/code.mdx (modified, 2 changes)\n  Patch: @@ -50,7 +50,7 @@ Code is a component used to display inline code.
 
 <CodeDemo title="Colors" files={codeContent.colors} />
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## API
 \n- apps/docs/content/docs/components/date-input.mdx (modified, 2 changes)\n  Patch: @@ -247,7 +247,7 @@ import {parseZonedDateTime} from "@internationalized/date";
 - **description**: The description of the date-input.
 - **errorMessage**: The error message of the date-input.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Data Attributes
 \n- apps/docs/content/docs/components/date-picker.mdx (modified, 6 changes)\n  Patch: @@ -270,7 +270,7 @@ import {I18nProvider} from "@react-aria/i18n";
 - **timeInputLabel**: The time-input component's label element.
 - **timeInput**: The time-input component element.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Data Attributes
 
@@ -289,7 +289,7 @@ import {I18nProvider} from "@react-aria/i18n";
 - **data-disabled**:
   When the date-picker is disabled. Based on `isDisabled` prop.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Accessibility
 
@@ -299,7 +299,7 @@ import {I18nProvider} from "@react-aria/i18n";
 - Date segments are editable using an easy to use numeric keypad, and all interactions are accessible using touch-based screen readers.
 - Integrates with HTML forms, supporting required, minimum and maximum values, unavailable dates, custom validation functions, realtime validation, and server-side validation errors
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## API
 \n- apps/docs/content/docs/components/input.mdx (modified, 12 changes)\n  Patch: @@ -85,14 +85,14 @@ You can change the position of the label by setting the `labelPlacement` propert
 
 You can use the `type` property to change the input type to `password`.
 
-<CodeDemo title="Password Input" highlightedLines="24" files={inputContent.password} />
+<CodeDemo title="Password Input" highlightedLines="25" files={inputContent.password} />
 
 ### Clear Button
 
 If you pass the `isClearable` property to the input, it will have a clear button at the
 end of the input, it will be visible when the input has a value.
 
-<CodeDemo title="Clear Button" highlightedLines="6" files={inputContent.clearButton} />
+<CodeDemo title="Clear Button" highlightedLines="7" files={inputContent.clearButton} />
 
 ### Start & End Content
 
@@ -142,7 +142,7 @@ You can use the `value` and `onValueChange` properties to control the input valu
 
 You can customize the `Input` component by passing custom Tailwind CSS classes to the component slots.
 
-<CodeDemo title="Custom Styles" highlightedLines="14-32" files={inputContent.customStyles} />
+<CodeDemo title="Custom Styles" highlightedLines="13-31" files={inputContent.customStyles} />
 
 ### Custom Implementation
 
@@ -155,7 +155,7 @@ In case you need to customize the input even further, you can use the `useInput`
   files={inputContent.customImpl}
 />
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Data Attributes
 
@@ -178,7 +178,7 @@ In case you need to customize the input even further, you can use the `useInput`
 - **data-disabled**:
   When the input is disabled. Based on `isDisabled` prop.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Accessibility
 
@@ -188,7 +188,7 @@ In case you need to customize the input even further, you can use the `useInput`
 - Required and invalid states exposed to assistive technology via ARIA.
 - Support for description and error message help text linked to the input via ARIA.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## API
 \n- apps/docs/content/docs/components/link.mdx (modified, 6 changes)\n  Patch: @@ -104,7 +104,7 @@ In case you need to customize the link even further, you can use the `useLink` h
 
 <CodeDemo showPreview={false} title="Custom implementation" files={linkContent.customImpl} />
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Data Attributes
 
@@ -117,7 +117,7 @@ In case you need to customize the link even further, you can use the `useLink` h
 - **data-disabled**:
   When the link is disabled. Based on `isDisabled` prop.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Accessibility
 
@@ -126,7 +126,7 @@ In case you need to customize the link even further, you can use the `useLink` h
 - Support for disabled links.
 - Keyboard users may activate links using the <Kbd>Enter</Kbd> key.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## API
 \n- apps/docs/content/docs/components/radio-group.mdx (modified, 6 changes)\n  Patch: @@ -100,7 +100,7 @@ In case you need to customize the radio group even further, you can use the `use
 
 <CodeDemo title="Custom Implementation" files={radioGroupContent.customImpl} />
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Data Attributes
 
@@ -130,7 +130,7 @@ In case you need to customize the radio group even further, you can use the `use
   - **data-disabled**:
     When the radio is disabled. Based on `isDisabled` prop.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Accessibility
 
@@ -141,7 +141,7 @@ In case you need to customize the radio group even further, you can use the `use
 - Keyboard focus management and cross browser normalization.
 - Group and radio labeling support for assistive technology.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## API
 \n- apps/docs/content/docs/components/snippet.mdx (modified, 2 changes)\n  Patch: @@ -92,7 +92,7 @@ You can customize the copy and copied icons by using the `copyIcon` and `checkIc
 - **copyIcon**: The copy icon slot.
 - **checkIcon**: The check icon slot.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## API
 \n- apps/docs/content/docs/components/spinner.mdx (modified, 2 changes)\n  Patch: @@ -68,7 +68,7 @@ Spinner express an unspecified wait time or display the length of a process.
 - **circle2**: The second circle of the spinner.
 - **label**: The label content.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## API
 \n- apps/docs/content/docs/components/switch.mdx (modified, 6 changes)\n  Patch: @@ -97,7 +97,7 @@ In case you need to customize the switch even further, you can use the `useSwitc
 
 <CodeDemo title="Custom Implementation" files={switchContent.customImpl} />
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Data Attributes
 
@@ -118,7 +118,7 @@ In case you need to customize the switch even further, you can use the `useSwitc
 - **data-disabled**:
   When the switch is disabled. Based on `isDisabled` prop.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Accessibility
 
@@ -129,7 +129,7 @@ In case you need to customize the switch even further, you can use the `useSwitc
 - Labeling support for assistive technology.
 - Exposed as a switch to assistive technology via ARIA
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## API
 \n- apps/docs/content/docs/components/textarea.mdx (modified, 6 changes)\n  Patch: @@ -99,7 +99,7 @@ You can use the `value` and `onValueChange` properties to control the input valu
 - **description**: The description of the textarea.
 - **errorMessage**: The error message of the textarea.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Data Attributes
 
@@ -120,7 +120,7 @@ You can use the `value` and `onValueChange` properties to control the input valu
 - **data-disabled**:
   When the textarea is disabled. Based on `isDisabled` prop.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## Accessibility
 
@@ -130,7 +130,7 @@ You can use the `value` and `onValueChange` properties to control the input valu
 - Required and invalid states exposed to assistive technology via ARIA.
 - Support for description and error message help text linked to the input via ARIA.
 
-<Spacer y={4} />{" "}
+<Spacer y={4} />
 
 ## API
 \n- apps/docs/content/docs/customization/theme.mdx (modified, 7 changes)\n  Patch: @@ -28,14 +28,19 @@ app's look and feel.
 The first step to using NextUI's theming capability is adding the `nextui` plugin to your
 `tailwind.config.js` file. Below is an example of how to do this:
 
-```js {8,13-14}
+<Blockquote color="primary">
+**Note**: If you are using pnpm and monorepo architecture, please make sure you are pointing to the ROOT `node_modules`
+</Blockquote>
+
+```js {9,14-15}
 // tailwind.config.js
 const {nextui} = require("@nextui-org/react");
 
 /** @type {import('tailwindcss').Config} */
 module.exports = {
   content: [
     // ...
+    // make sure it's pointing to the ROOT node_module
     "./node_modules/@nextui-org/theme/dist/**/*.{js,ts,jsx,tsx}",
   ],
   theme: {\n- apps/docs/content/docs/frameworks/astro.mdx (modified, 35 changes)\n  Patch: @@ -9,7 +9,7 @@ Requirements:
 
 - [React 18](https://reactjs.org/) or later
 - [Tailwind CSS 3.4](https://tailwindcss.com/docs/guides/astro) or later
-- [Framer Motion 4](https://www.framer.com/motion/) or later
+- [Framer Motion 11](https://www.framer.com/motion/) or later
 
 ------
 
@@ -34,28 +34,44 @@ In your Astro project, run one of the following command to install NextUI:
     npm: 'npm i @nextui-org/react framer-motion',
     yarn: 'yarn add @nextui-org/react framer-motion',
     pnpm: 'pnpm add @nextui-org/react framer-motion',
+    bun: "bun add @nextui-org/react framer-motion"
   }}
 />
 
+### Hoisted Dependencies Setup
 
-<Spacer y={4}/>
+<Blockquote color="primary">
+**Note**: This step is only for those who use pnpm to install. If you install NextUI using other package managers, you may skip this step.
+</Blockquote>
+
+If you are using pnpm, you need to add the following line to your `.npmrc` file to hoist our packages to the root `node_modules`.
+
+```bash
+public-hoist-pattern[]=*@nextui-org/*
+```
+
+After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
 
 ### Tailwind CSS Setup
 
 NextUI is built on top of Tailwind CSS, so you need to install Tailwind CSS first. You can follow the official
 [installation guide](https://tailwindcss.com/docs/guides/astro) to install Tailwind CSS. Then you need to add 
 the following code to your `tailwind.config.cjs` file:
 
+<Blockquote color="primary">
+**Note**: If you are using pnpm and monorepo architecture, please make sure you are pointing to the ROOT `node_modules`
+</Blockquote>
 
-```js {8,13-14}
+```js {9,14-15}
 // tailwind.config.cjs
 const { nextui } = require("@nextui-org/react");
 
 /** @type {import('tailwindcss').Config} */
 module.exports = {
   content: [
     // ...
-    "./node_modules/@nextui-org/theme/dist/**/*.{js,ts,jsx,tsx}"
+    // make sure it's pointing to the ROOT node_module
+    "./node_modules/@nextui-org/theme/dist/**/*.{js,ts,jsx,tsx}",
   ],
   theme: {
     extend: {},
@@ -85,17 +101,6 @@ import {Button} from '@nextui-org/react';
 Note that you have to add `client:visible` to the component to make it visible only on the client side. Otherwise
 some functionalities of NextUI components may not work properly.
 
-
-### Setup pnpm (optional)
-
-If you are using pnpm, you need to add the following code to your `.npmrc` file:
-
-```bash
-public-hoist-pattern[]=*@nextui-org/*
-```
-
-After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
-
 </Steps>
 
 <Blockquote\n- apps/docs/content/docs/frameworks/nextjs.mdx (modified, 91 changes)\n  Patch: @@ -11,7 +11,7 @@ Requirements:
 - [Next.js 12](https://nextjs.org/) or later
 - [React 18](https://reactjs.org/) or later
 - [Tailwind CSS 3.4](https://tailwindcss.com/docs/guides/nextjs) or later
-- [Framer Motion 4](https://www.framer.com/motion/) or later
+- [Framer Motion 11](https://www.framer.com/motion/) or later
 
 ------
 
@@ -25,7 +25,7 @@ Next.js 13 introduces a new `app/` directory structure. By default it uses Serve
 As NextUI components use React hooks, we added the `use client;` at build time, so you can import them
 directly in your React Server Components (RSC).
 
-## NextUI CLI (recommended)
+### NextUI CLI (recommended)
 
 If you are starting a new project, you can use the NextUI CLI to create a new project with NextUI pre-configured:
 
@@ -51,8 +51,7 @@ If you are starting a new project, you can run one of the following commands to
   }}
 />
 
-
-## Automatic Installation
+### Automatic Installation
 
 You can add individual components using the CLI. For example, to add a button component:
 
@@ -100,7 +99,7 @@ Filtered results for: Enter something to filter
 
 You still need to add the provider to your app manually (we are working on automating this step).
 
-```jsx
+```jsx {7,9}
 // app/providers.tsx
 
 import {NextUIProvider} from '@nextui-org/react'
@@ -114,7 +113,9 @@ export function Providers({children}: { children: React.ReactNode }) {
 }
 ```
 
-```jsx
+<Spacer y={4} />
+
+```jsx {7,9,11}
 // app/layout.tsx
 
 import {Providers} from "./providers";
@@ -132,7 +133,6 @@ export default function RootLayout({children}: { children: React.ReactNode }) {
 }
 ```
 
-
 ### Manual Installation
 
 <Steps>
@@ -146,24 +146,43 @@ In your Next.js project, run one of the following commands to install NextUI:
     npm: 'npm i @nextui-org/react framer-motion',
     yarn: 'yarn add @nextui-org/react framer-motion',
     pnpm: 'pnpm add @nextui-org/react framer-motion',
+    bun: "bun add @nextui-org/react framer-motion"
   }}
 />
 
+### Hoisted Dependencies Setup
+
+<Blockquote color="primary">
+**Note**: This step is only for those who use pnpm to install. If you install NextUI using other package managers, you may skip this step.
+</Blockquote>
+
+If you are using pnpm, you need to add the following line to your `.npmrc` file to hoist our packages to the root `node_modules`.
+
+```bash
+public-hoist-pattern[]=*@nextui-org/*
+```
+
+After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
+
 ### Tailwind CSS Setup
 
 NextUI is built on top of Tailwind CSS, so you need to install Tailwind CSS first. You can follow the official
 [installation guide](https://tailwindcss.com/docs/guides/nextjs) to install Tailwind CSS. Then you need to add 
 the following code to your `tailwind.config.js` file:
 
+<Blockquote color="primary">
+**Note**: If you are using pnpm and monorepo architecture, please make sure you are pointing to the ROOT `node_modules`
+</Blockquote>
 
-```js {8,13-14}
+```js {9,14-15}
 // tailwind.config.js
 import {nextui} from "@nextui-org/react";
 
 /** @type {import('tailwindcss').Config} */
 const config = {
   content: [
     // ...
+    // make sure it's pointing to the ROOT node_module
     "./node_modules/@nextui-org/theme/dist/**/*.{js,ts,jsx,tsx}"
   ],
   theme: {
@@ -180,7 +199,7 @@ export default config;
 
 Go to your `app/providers.tsx` or `app/providers.jsx` (create it if it doesn't exist) and wrap the Component with the `NextUIProvider`:
 
-```jsx
+```jsx {8,10}
 // app/providers.tsx
 'use client'
 
@@ -239,20 +258,8 @@ export default function Page() {
 ```
 > **Important 🚨**: Note that you need to import the component from the individual package, not from `@nextui-org/react`. 
 
-### Setup pnpm (optional)
-
-If you are using pnpm, you need to add the following code to your `.npmrc` file:
-
-```bash
-public-hoist-pattern[]=*@nextui-org/*
-```
-
-After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
-
 </Steps>
 
-<Spacer y={8} />
-
 ## Pages Directory Setup 
 
 ### NextUI CLI (recommended)
@@ -331,7 +338,7 @@ Filtered results for: Enter something to filter
 
 You still need to add the provider to your app manually (we are working on automating this step).
 
-```jsx
+```jsx {7,9}
 // app/providers.tsx
 
 import {NextUIProvider} from '@nextui-org/react'
@@ -345,7 +352,9 @@ export function Providers({children}: { children: React.ReactNode }) {
 }
 ```
 
-```jsx
+<Spacer y={4} />
+
+```jsx {7,9,11}
 // app/layout.tsx
 
 import {Providers} from "./providers";
@@ -376,24 +385,43 @@ In your Next.js project, run one of the following commands to install NextUI:
     npm: 'npm i @nextui-org/react framer-motion',
     yarn: 'yarn add @nextui-org/react framer-motion',
     pnpm: 'pnpm add @nextui-org/react framer-motion',
+    bun: "bun add @nextui-org/react framer-motion"
   }}
 />
 
+### Hoisted Dependencies Setup
+
+<Blockquote color="primary">
+**Note**: This step is only for those who use pnpm to install. If you install NextUI using other package managers, you may skip this step.
+</Blockquote>
+
+If you are using pnpm, you need to add the following line to your `.npmrc` file to hoist our packages to the root `node_modules`.
+
+```bash
+public-hoist-pattern[]=*@nextui-org/*
+```
+
+After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
+
 ### Tailwind CSS Setup
 
 NextUI is built on top of Tailwind CSS, so you need to install Tailwind CSS first. You can follow the official
 [installation guide](https://tailwindcss.com/docs/guides/nextjs) to install Tailwind CSS. Then you need to add 
 the following code to your `tailwind.config.js` file:
 
+<Blockquote color="primary">
+**Note**: If you are using pnpm and monorepo architecture, please make sure you are pointing to the ROOT `node_modules`
+</Blockquote>
 
-```js {8,13-14}
+```js {9,14-15}
 // tailwind.config.js
 import {nextui} from "@nextui-org/react";
 
 /** @type {import('tailwindcss').Config} */
 const config = {
   content: [
     // ...
+    // make sure it's pointing to the ROOT node_module
     "./node_modules/@nextui-org/theme/dist/**/*.{js,ts,jsx,tsx}"
   ],
   theme: {
@@ -410,7 +438,7 @@ export default config;
 
 Go to pages`/_app.js` or `pages/_app.tsx` (create it if it doesn't exist) and wrap the Component with the `NextUIProvider`:
 
-```jsx
+```jsx {6,8}
 // pages/_app.js
 import {NextUIProvider} from '@nextui-org/react'
 
@@ -429,7 +457,7 @@ export default MyApp;
 
 Now you can import any NextUI component wherever you want:
 
-```jsx
+```jsx {1}
 import {Button} from '@nextui-org/react'
 
 export default function Page() {
@@ -440,17 +468,6 @@ export default function Page() {
   )
 }
 ```
-
-### Setup pnpm (optional)
-
-If you are using pnpm, you need to add the following code to your `.npmrc` file:
-
-```bash
-public-hoist-pattern[]=*@nextui-org/*
-```
-
-After modifying the `.npmrc` file, you must run `pnpm install` again to ensure the dependencies are installed correctly.
-
 </Steps>
 
 <Blockquote\n- apps/docs/content/docs/frameworks/remix.mdx (modified, 32 changes)\n  Patch: @@ -9,7 +9,7 @@ Requirements:
 
 - [React 18](https://reactjs.org/) or later
 - [Tailwind CSS 3.4](https://tailwindcss.com/docs/guides/remix) or later
-- [Framer Motion 4](https://www.framer.com/motion/) or later
+- [Framer Motion 11](https://www.framer.com/motion/) or later
 
 ------
 
@@ -28,18 +28,35 @@ In your Remix project, run one of the following command to install NextUI:
     npm: 'npm i @nextui-org/react framer-motion',
     yarn: 'yarn add @nextui-org/react framer-motion',
     pnpm: 'pnpm add @nextui-org/react framer-motion',
+    bun: "bun add @nextui-org/react framer-motion"
   }}
 />
 
+### Hoisted Dependencies Setup
+
+<Blockquote color="primary">
+**Note**: This step is only for those who use pnpm to install. If you install NextUI using other package managers, you may skip this step.
+</Blockquote>
+
+If you are using pnpm, you need to add the following line to your `.npmrc` file to hoist our packages to the root `node_modules`.
+
+```bash
+public-hoist-pattern[]=*@nextui-org/*
+```
+
+After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
 
 ### Tailwind CSS Setup
 
 NextUI is built on top of Tailwind CSS, so you need to install Tailwind CSS first. You can follow the official
 [installation guide](https://tailwindcss.com/docs/guides/remix) to install Tailwind CSS. Then you need to add 
 the following code to your `tailwind.config.js` file:
 
+<Blockquote color="primary">
+**Note**: If you are using pnpm and monorepo architecture, please make sure you are pointing to the ROOT `node_modules`
+</Blockquote>
 
-```ts {9,14-15}
+```ts {10,15-16}
 // tailwind.config.ts
 const { nextui } = require("@nextui-org/react");
 
@@ -48,6 +65,7 @@ import type { Config } from 'tailwindcss'
 export default {
   content: [
     // ...
+    // make sure it's pointing to the ROOT node_module
     "./node_modules/@nextui-org/theme/dist/**/*.{js,ts,jsx,tsx}"
   ],
   theme: {
@@ -95,16 +113,6 @@ export default function App() {
 }
 ```
 
-### Setup pnpm (optional)
-
-If you are using pnpm, you need to add the following code to your `.npmrc` file:
-
-```bash
-public-hoist-pattern[]=*@nextui-org/*
-```
-
-After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
-
 </Steps>
 
 <Blockquote\n- apps/docs/content/docs/frameworks/vite.mdx (modified, 30 changes)\n  Patch: @@ -10,7 +10,7 @@ Requirements:
 - [Vite 2](https://vitejs.dev/) or later
 - [React 18](https://reactjs.org/) or later
 - [Tailwind CSS 3.4](https://tailwindcss.com/docs/guides/vite#react) or later
-- [Framer Motion 4](https://www.framer.com/motion/) or later
+- [Framer Motion 11](https://www.framer.com/motion/) or later
 
 ------
 
@@ -29,15 +29,33 @@ In your Vite React project, run one of the following command to install NextUI:
     npm: 'npm i @nextui-org/react framer-motion',
     yarn: 'yarn add @nextui-org/react framer-motion',
     pnpm: 'pnpm add @nextui-org/react framer-motion',
+    bun: "bun add @nextui-org/react framer-motion"
   }}
 />
 
+### Hoisted Dependencies Setup
+
+<Blockquote color="primary">
+**Note**: This step is only for those who use pnpm to install. If you install NextUI using other package managers, you may skip this step.
+</Blockquote>
+
+If you are using pnpm, you need to add the following line to your `.npmrc` file to hoist our packages to the root `node_modules`.
+
+```bash
+public-hoist-pattern[]=*@nextui-org/*
+```
+
+After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
+
 ### Tailwind CSS Setup
 
 NextUI is built on top of Tailwind CSS, so you need to install Tailwind CSS first. You can follow the official
 [installation guide](https://tailwindcss.com/docs/guides/vite#react) to install Tailwind CSS. Then you need to add 
 the following code to your `tailwind.config.js` file:
 
+<Blockquote color="primary">
+**Note**: If you are using pnpm and monorepo architecture, please make sure you are pointing to the ROOT `node_modules`
+</Blockquote>
 
 ```js {8,13-14}
 // tailwind.config.js
@@ -80,16 +98,6 @@ ReactDOM.createRoot(document.getElementById('root')).render(
 )
 ```
 
-### Setup pnpm (optional)
-
-If you are using pnpm, you need to add the following code to your `.npmrc` file:
-
-```bash
-public-hoist-pattern[]=*@nextui-org/*
-```
-
-After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
-
 </Steps>
 
 <Blockquote\n- apps/docs/content/docs/guide/installation.mdx (modified, 67 changes)\n  Patch: @@ -9,7 +9,7 @@ Requirements:
 
 - [React 18](https://reactjs.org/) or later
 - [Tailwind CSS 3.4](https://tailwindcss.com/) or later
-- [Framer Motion 4](https://www.framer.com/motion/) or later
+- [Framer Motion 11](https://www.framer.com/motion/) or later
 
 ---
 
@@ -94,11 +94,11 @@ Filtered results for: Enter something to filter
 
 ## Manual Installation
 
-If you prefer not to use the CLI, follow these steps to manually set up NextUI in your project:
+If you prefer not to use the CLI, you may try either global installation or individual installation to set up NextUI in your project:
 
 ### Global Installation
 
-The easiest way to get started with NextUI is to use the global installation. Which means
+The easiest way to get started with NextUI is to use the global installation, which means
 that all the components are imported from a single package.
 
 Follow the steps below to install all NextUI components:
@@ -118,20 +118,39 @@ To install NextUI, run one of the following commands in your terminal:
   }}
 />
 
+#### Hoisted Dependencies Setup
+
+<Blockquote color="primary">
+**Note**: This step is only for those who use pnpm to install. If you install NextUI using other package managers, you may skip this step.
+</Blockquote>
+
+If you are using pnpm, you need to add the following line to your `.npmrc` file to hoist our packages to the root `node_modules`.
+
+```bash
+public-hoist-pattern[]=*@nextui-org/*
+```
+
+After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
+
 #### Tailwind CSS Setup
 
 NextUI is built on top of Tailwind CSS, so you need to install Tailwind CSS first. You can follow the official
 [installation guide](https://tailwindcss.com/docs/installation) to install Tailwind CSS. Then you need to add
 the following code to your `tailwind.config.js` file:
 
-```js {8,13-14}
+<Blockquote color="primary">
+**Note**: If you are using pnpm and monorepo architecture, please make sure you are pointing to the ROOT `node_modules`
+</Blockquote>
+
+```js {9,14-15}
 // tailwind.config.js
 const {nextui} = require("@nextui-org/react");
 
 /** @type {import('tailwindcss').Config} */
 module.exports = {
   content: [
     // ...
+    // make sure it's pointing to the ROOT node_module
     "./node_modules/@nextui-org/theme/dist/**/*.{js,ts,jsx,tsx}",
   ],
   theme: {
@@ -162,16 +181,6 @@ function App() {
 }
 ```
 
-#### Setup pnpm (optional)
-
-If you are using pnpm, you need to add the following code to your `.npmrc` file:
-
-```bash
-public-hoist-pattern[]=*@nextui-org/*
-```
-
-After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
-
 </Steps>
 
 ### Individual Installation
@@ -180,7 +189,9 @@ NextUI is also available as individual packages. You can install each package se
 is useful if you want to reduce the size of your CSS bundle as it will only include styles for the components
 you're actually using.
 
-> **Note**: JavaScript bundle size will not change due to tree shaking support in NextUI.
+<Blockquote color="primary">
+**Note**: JavaScript bundle size will not change due to tree shaking support in NextUI.
+</Blockquote>
 
 Follow the steps below to install each package separately:
 
@@ -217,6 +228,20 @@ in your terminal:
   }}
 />
 
+#### Hoisted Dependencies Setup
+
+<Blockquote color="primary">
+**Note**: This step is only for those who use pnpm to install. If you install NextUI using other package managers, you may skip this step.
+</Blockquote>
+
+If you are using pnpm, you need to add the following line to your `.npmrc` file to hoist our packages to the root `node_modules`.
+
+```bash
+public-hoist-pattern[]=*@nextui-org/*
+```
+
+After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
+
 #### Tailwind CSS Setup
 
 TailwindCSS setup changes a bit when you use individual packages. You only need to add the
@@ -278,18 +303,6 @@ function App() {
   );
 }
 ```
-
-
-#### Setup pnpm (optional)
-
-If you are using pnpm, you need to add the following code to your `.npmrc` file:
-
-```bash
-public-hoist-pattern[]=*@nextui-org/*
-```
-
-After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
-
 </Steps>
 
 <Blockquote color="warning">\n- apps/docs/content/docs/guide/upgrade-to-v2.mdx (modified, 88 changes)\n  Patch: @@ -10,7 +10,7 @@ Requirements:
 
 - [React 18](https://reactjs.org/) or later
 - [Tailwind CSS 3.4](https://tailwindcss.com/) or later
-- [Framer Motion 4](https://www.framer.com/motion/) or later
+- [Framer Motion 11](https://www.framer.com/motion/) or later
 
 ----- 
 
@@ -42,21 +42,36 @@ In your Next.js project, run one of the following command to install NextUI:
   }}
 />
 
+### Hoisted Dependencies Setup
+
+<Blockquote color="primary">
+**Note**: This step is only for those who use pnpm to install. If you install NextUI using other package managers, you may skip this step.
+</Blockquote>
+
+If you are using pnpm, you need to add the following line to your `.npmrc` file to hoist our packages to the root `node_modules`.
+
+```bash
+public-hoist-pattern[]=*@nextui-org/*
+```
+
+After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
+
 ### Tailwind CSS Setup
 
 NextUI is built on top of Tailwind CSS, so you need to install Tailwind CSS first. You can follow the official
 [installation guide](https://tailwindcss.com/docs/guides/nextjs) to install Tailwind CSS. Then you need to add 
 the following code to your `tailwind.config.js` file:
 
 
-```js {8,13-14}
+```js {9,14-15}
 // tailwind.config.js
 const { nextui } = require("@nextui-org/react");
 
 /** @type {import('tailwindcss').Config} */
 module.exports = {
   content: [
     // ...
+    // make sure it's pointing to the ROOT node_module
     "./node_modules/@nextui-org/theme/dist/**/*.{js,ts,jsx,tsx}"
   ],
   theme: {
@@ -131,16 +146,6 @@ export default function Page() {
 
 > **Important 🚨**: Note that you need to import the component from the individual package, not the from `@nextui-org/react`. 
 
-### Setup pnpm (optional)
-
-If you are using pnpm, you need to add the following code to your `.npmrc` file:
-
-```bash
-public-hoist-pattern[]=*@nextui-org/*
-```
-
-After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
-
 </Steps>
 
 <Spacer y={8} />
@@ -162,21 +167,36 @@ In your Next.js project, run one of the following command to install NextUI:
   }}
 />
 
+### Hoisted Dependencies Setup
+
+<Blockquote color="primary">
+**Note**: This step is only for those who use pnpm to install. If you install NextUI using other package managers, you may skip this step.
+</Blockquote>
+
+If you are using pnpm, you need to add the following line to your `.npmrc` file to hoist our packages to the root `node_modules`.
+
+```bash
+public-hoist-pattern[]=*@nextui-org/*
+```
+
+After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
+
 ### Tailwind CSS Setup
 
 NextUI is built on top of Tailwind CSS, so you need to install Tailwind CSS first. You can follow the official
 [installation guide](https://tailwindcss.com/docs/guides/nextjs) to install Tailwind CSS. Then you need to add 
 the following code to your `tailwind.config.js` file:
 
 
-```js {8,13-14}
+```js {9,14-15}
 // tailwind.config.js
 const { nextui } = require("@nextui-org/react");
 
 /** @type {import('tailwindcss').Config} */
 module.exports = {
   content: [
     // ...
+    // make sure it's pointing to the ROOT node_module
     "./node_modules/@nextui-org/theme/dist/**/*.{js,ts,jsx,tsx}"
   ],
   theme: {
@@ -222,16 +242,6 @@ export default function Page() {
 }
 ```
 
-### Setup pnpm (optional)
-
-If you are using pnpm, you need to add the following code to your `.npmrc` file:
-
-```bash
-public-hoist-pattern[]=*@nextui-org/*
-```
-
-After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
-
 </Steps>
 
 ## React upgrade steps
@@ -250,7 +260,6 @@ NextUI v2 requires React 18 or later. To upgrade React, run the following comman
   }}
 />
 
-
 ### Install Framer motion
 
 In v2, NextUI now requires `framer-motion` as a dependency. To install both, use the following command:
@@ -263,18 +272,37 @@ In v2, NextUI now requires `framer-motion` as a dependency. To install both, use
   }}
 />
 
+### Hoisted Dependencies Setup
+
+<Blockquote color="primary">
+**Note**: This step is only for those who use pnpm to install. If you install NextUI using other package managers, you may skip this step.
+</Blockquote>
+
+If you are using pnpm, you need to add the following line to your `.npmrc` file to hoist our packages to the root `node_modules`.
+
+```bash
+public-hoist-pattern[]=*@nextui-org/*
+```
+
+After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
+
 ### TailwindCSS Setup
 
 NextUI v2 now uses Tailwind CSS. Add the NextUI plugin to your `tailwind.config.js` file:
 
-```js {9, 13-14}
+<Blockquote color="primary">
+**Note**: If you are using pnpm and monorepo architecture, please make sure you are pointing to the ROOT `node_modules`
+</Blockquote>
+
+```js {9,14-15}
 // tailwind.config.js
 const { nextui } = require("@nextui-org/react");
 
 /** @type {import('tailwindcss').Config} */
 module.exports = {
   content: [
     // ...
+    // make sure it's pointing to the ROOT node_module
     "./node_modules/@nextui-org/theme/dist/**/*.{js,ts,jsx,tsx}"
   ],
   theme: {
@@ -322,16 +350,6 @@ export default function Page() {
 }
 ```
 
-### Setup pnpm (optional)
-
-If you are using pnpm, you need to add the following code to your `.npmrc` file:
-
-```bash
-public-hoist-pattern[]=*@nextui-org/*
-```
-
-After modifying the `.npmrc` file, you need to run `pnpm install` again to ensure that the dependencies are installed correctly.
-
 </Steps>
 
 <Blockquote\n
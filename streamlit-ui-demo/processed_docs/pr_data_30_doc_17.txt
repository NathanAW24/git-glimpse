Pull Request Number: 259
Title: Fix/documentation issues
Base Branch: staging
Head Branch: fix/documentation-issues
Author: jrgarciadev
URL: https://github.com/nextui-org/nextui/pull/259
State: MERGED
Created At: 2022-02-09T03:16:53Z
Merged At: 2022-02-09T03:18:00Z
Participants: jrgarciadev

Description:
[docs]
Types of changes


 Bug fix (non-breaking change which fixes an issue)
 New feature (non-breaking change which adds functionality)
 Breaking change (fix or feature that would cause existing functionality to not work as expected)
 Documentation Only
 Refactor

Description, Motivation and Context
docs typos, clear examples, inline styles removed



Screenshots - Animations

Commits:
- fix: docs typos, clear examples, inline styles removed\n- Merge branch 'staging' of github.com:nextui-org/nextui into fix/documentation-issues\n- chore(docs): left sidebar vertical padding increased\n

Labels:


Comments:
No comments.

Files Changed:
- apps/docs/content/docs/components/avatar.mdx (modified, 4 changes)\n  Patch: @@ -99,7 +99,7 @@ import { Avatar } from '@nextui-org/react';
 
 <Playground
   title="Bordered"
-  desc="You can change the full style to a bodered `Avatar` with the `bordered` property."
+  desc="You can change the full style to a bordered `Avatar` with the `bordered` property."
   code={`
   <Grid.Container gap={2}>
     <Grid>
@@ -272,7 +272,7 @@ import { Avatar } from '@nextui-org/react';
 | **text**         | `string`                | -                                        | Display text when image is missing     | -         |
 | **alt**          | `string`                | -                                        | Display alt text when image is missing | -         |
 | **size**         | `NormalSizes` `number`  | [NormalSizes](#normal-sizes)             | Avatar size                            | `medium`  |
-| **bordered**     | `boolean`               | `true/false`                             | Bodered avatar                         | `false`   |
+| **bordered**     | `boolean`               | `true/false`                             | Bordered avatar                        | `false`   |
 | **borderWeight** | `NormalWeights`         | [NormalWeights](#normal-weights)         | Border weight for `bordered` avatar    | `normal`  |
 | **pointer**      | `boolean`               | `true/false`                             | Display pointer cursor on hover        | `false`   |
 | **stacked**      | `boolean`               | `true/false`                             | Stacked display group                  | `false`   |\n- apps/docs/content/docs/components/button-group.mdx (modified, 2 changes)\n  Patch: @@ -201,7 +201,7 @@ import { Button } from '@nextui-org/react';
 | **color**        | `NormalColors`         | [NormalColors](#normal-colors)   | Change buttons color                   | `default` |
 | **size**         | `NormalSizes`          | [NormalSizes](#normal-sizes)     | Change buttons size                    | `medium`  |
 | **disabled**     | `boolean`              | `true/false`                     | Disable all buttons                    | `false`   |
-| **bordered**     | `boolean`              | `true/false`                     | Bodered buttons                        | `false`   |
+| **bordered**     | `boolean`              | `true/false`                     | Bordered buttons                       | `false`   |
 | **flat**         | `boolean`              | `true/false`                     | Flat buttons                           | `false`   |
 | **rounded**      | `boolean`              | `true/false`                     | Rounded buttons                        | `false`   |
 | **ghost**        | `boolean`              | `true/false`                     | The opposite color                     | `false`   |\n- apps/docs/content/docs/components/button.mdx (modified, 10 changes)\n  Patch: @@ -166,7 +166,7 @@ import { Button } from '@nextui-org/react';
 
 <Playground
   title="Bordered"
-  desc="You can change the full style towards a bodered `Button` with the `bordered` property."
+  desc="You can change the full style to a bordered `Button` with the `bordered` property."
   code={`
   <Grid.Container gap={2}>
   <Grid>
@@ -244,7 +244,7 @@ import { Button } from '@nextui-org/react';
 
 <Playground
   title="Ghost"
-  desc="You can change the full style towards a ghost button with the `ghost` property."
+  desc="You can change the full style to a ghost button with the `ghost` property."
   code={`
   <Grid.Container gap={2}>
   <Grid>
@@ -283,7 +283,7 @@ import { Button } from '@nextui-org/react';
 
 <Playground
   title="Flat"
-  desc="You can change the full style towards a flat button with the `flat` property.
+  desc="You can change the full style to a flat button with the `flat` property.
                                         
 **_Note: `gradient` variant is not supported with the `flat` mode._**"
   code={`
@@ -319,7 +319,7 @@ import { Button } from '@nextui-org/react';
 
 <Playground
   title="Light"
-  desc="You can change the full style towards a light button with the `light` property. 
+  desc="You can change the full style to a light button with the `light` property. 
   
   ***_Note: `gradient` variant is not supported with the `light` mode._***"
   code={`
@@ -395,7 +395,7 @@ import { Button } from '@nextui-org/react';
 | **size**         | `NormalSizes`                                   | [NormalSizes](#normal-sizes)     | Change button size                        | `medium`  |
 | **disabled**     | `boolean`                                       | `true/false`                     | Disable button                            | `false`   |
 | **clickable**    | `boolean`                                       | `true/false`                     | Inidicates if button is clickable         | `false`   |
-| **bordered**     | `boolean`                                       | `true/false`                     | Bodered button                            | `false`   |
+| **bordered**     | `boolean`                                       | `true/false`                     | Bordered button                           | `false`   |
 | **flat**         | `boolean`                                       | `true/false`                     | Flat button                               | `false`   |
 | **rounded**      | `boolean`                                       | `true/false`                     | Rounded button                            | `false`   |
 | **ghost**        | `boolean`                                       | `true/false`                     | The opposite color                        | `false`   |\n- apps/docs/content/docs/components/card.mdx (modified, 14 changes)\n  Patch: @@ -27,7 +27,7 @@ import { Card } from '@nextui-org/react';
 
 <Playground
   title="Bordered"
-  desc="You can change the full style towards a bodered `Card` with the `bordered` property."
+  desc="You can change the full style to a bordered `Card` with the `bordered` property."
   code={`
 <Card bordered shadow={false} css={{ mw: "400px" }}>
   <p>A bordered card.</p>
@@ -168,6 +168,7 @@ return (
   applies `object-fit: cover` to the inner image."
   code={`
 <Grid.Container gap={2} justify="center">
+   {/* Card 1 */}
     <Grid xs={12} sm={4}>
       <Card cover>
         <Card.Header css={{ position: 'absolute', zIndex: 1, top: 5 }}>
@@ -193,6 +194,7 @@ return (
         />
       </Card>
     </Grid>
+    {/* Card 2 */}
     <Grid xs={12} sm={4}>
       <Card width="100%" cover>
         <Card.Header css={{ position: 'absolute', zIndex: 1, top: 5 }}>
@@ -218,8 +220,9 @@ return (
         />
       </Card>
     </Grid>
+    {/* Card 3 */}
     <Grid xs={12} sm={4}>
-      <Card cover css={{ bg: '$gray100', w: '100%' }}>
+      <Card cover css={{ bg: '$black', w: '100%' }}>
         <Card.Header css={{ position: 'absolute', zIndex: 1, top: 5 }}>
           <Col>
             <Text
@@ -243,6 +246,7 @@ return (
         />
       </Card>
     </Grid>
+    {/* Card 4 */}
     <Grid xs={12} sm={5}>
       <Card cover css={{ w: '100%' }}>
         <Card.Header css={{ position: 'absolute', zIndex: 1, top: 5 }}>
@@ -296,6 +300,7 @@ return (
         </Card.Footer>
       </Card>
     </Grid>
+    {/* Card 5 */}
     <Grid xs={12} sm={7}>
       <Card cover css={{ w: '100%', p: 0 }}>
         <Card.Header css={{ position: 'absolute', zIndex: 1, top: 5 }}>
@@ -337,7 +342,7 @@ return (
                 <Col span={3}>
                   <Card.Image
                     src='/images/breathing-app-icon.jpeg'
-                    style={{ background: 'black' }}
+                    css={{ background: 'black' }}
                     height={40}
                     width={40}
                     alt="Breathing app icon"
@@ -366,6 +371,7 @@ return (
         </Card.Footer>
       </Card>
     </Grid>
+
 </Grid.Container>`}
 />
 
@@ -420,7 +426,7 @@ return (
 <Grid.Container gap={2} justify="flex-start">
   {list.map((item, index) => (
     <Grid xs={6} sm={3} key={index}>
-      <Card hoverable clickable width="100%">
+      <Card hoverable clickable>
         <Card.Body css={{ p: 0 }}>
           <Card.Image
             objectFit="cover"\n- apps/docs/content/docs/components/checkbox.mdx (modified, 2 changes)\n  Patch: @@ -124,7 +124,7 @@ import { Checkbox } from '@nextui-org/react';
 
 <Playground
   title="Rounded"
-  desc="You can change the full style towards a rounded `Checkbox` with the `rounded` property."
+  desc="You can change the full style to a rounded `Checkbox` with the `rounded` property."
   code={`
  <Checkbox rounded color="primary" checked={true}>
     Rounded option\n- apps/docs/content/docs/components/collapse.mdx (modified, 40 changes)\n  Patch: @@ -195,7 +195,7 @@ import { Collapse } from '@nextui-org/react';
 
 <Playground
   title="Bordered"
-  desc="You can change the full style towards a bodered `Collapse` with the `bordered` property."
+  desc="You can change the full style to a bordered `Collapse` with the `bordered` property."
   code={` 
 <Grid.Container gap={2}>
     <Grid>
@@ -245,7 +245,7 @@ import { Collapse } from '@nextui-org/react';
 
 <Playground
   title="Splitted"
-  desc="You can change the full style towards a separated `Collapse` with the `splitted` property."
+  desc="You can change the full style to a separated `Collapse` with the `splitted` property."
   code={` 
 <Grid.Container gap={2}>
     <Grid>
@@ -478,24 +478,24 @@ import { Collapse } from '@nextui-org/react';
 
 #### Collapse Props
 
-| Attribute           | Type                                                           | Accepted values                  | Description                                                               | Default |
-| ------------------- | -------------------------------------------------------------- | -------------------------------- | ------------------------------------------------------------------------- | ------- |
-| **expanded** | `boolean`                                                      | `true/false`                     | Manage the expand behaivor by prop                               | `false` |
-| **title**           | `string` `React.ReactNode`                                     | -                                | Collapse title content                                                    | -       |
-| **subtitle**        | `string` `React.ReactNode`                                     | -                                | Collapse description content                                              | -       |
-| **divider**         | `boolean`                                                      | `true/false`                     | Show or hide the collapse divider                                         | `true`  |
-| **disabled**        | `boolean`                                                      | `true/false`                     | Disable collapse                                                          | `false` |
-| **bordered**        | `boolean`                                                      | `true/false`                     | Bordered collapse                                                         | `false` |
-| **shadow**          | `boolean`                                                      | `true/false`                     | Enable or disable the collapse shadow                                     | `false` |
-| **animated**        | `boolean`                                                      | `true/false`                     | Enable or disable the collapse animation                                  | `true`  |
-| **showArrow**       | `boolean`                                                      | `true/false`                     | Show or hide the collapse right arrow                                     | `true`  |
-| **arrowIcon**       | `React.ReactNode`                                              | -                                | Collapse arrow icon                                                       | -       |
-| **contentLeft**     | `React.ReactNode`                                              | -                                | Left content for the collapse                                             | -       |
-| **borderWeight**    | `NormalWeights`                                                | [NormalWeights](#normal-weights) | Border weight for `bordered` collapse                                     | `light` |
-| **index**           | `number`                                                       | -                                | Collapse's index, this value it's autogenerated by default                | -       |
-| **preventDefault**  | `boolean`                                                      | `true/false`                     | Prevent default event on when is selected through the `Space, Enter` keys | `true`  |
-| **onChange**        | `(e: React.ChangeEvent, index:number, value: boolean) => void` | -                                | Callback fired when the value is changed                                  | -       |
-| ...                 | `HTMLAttributes`                                               | `'id', 'className', ...`         | Div native props                                                          | -       |
+| Attribute          | Type                                                           | Accepted values                  | Description                                                               | Default |
+| ------------------ | -------------------------------------------------------------- | -------------------------------- | ------------------------------------------------------------------------- | ------- |
+| **expanded**       | `boolean`                                                      | `true/false`                     | Manage the expand behaivor by prop                                        | `false` |
+| **title**          | `string` `React.ReactNode`                                     | -                                | Collapse title content                                                    | -       |
+| **subtitle**       | `string` `React.ReactNode`                                     | -                                | Collapse description content                                              | -       |
+| **divider**        | `boolean`                                                      | `true/false`                     | Show or hide the collapse divider                                         | `true`  |
+| **disabled**       | `boolean`                                                      | `true/false`                     | Disable collapse                                                          | `false` |
+| **bordered**       | `boolean`                                                      | `true/false`                     | Bordered collapse                                                         | `false` |
+| **shadow**         | `boolean`                                                      | `true/false`                     | Enable or disable the collapse shadow                                     | `false` |
+| **animated**       | `boolean`                                                      | `true/false`                     | Enable or disable the collapse animation                                  | `true`  |
+| **showArrow**      | `boolean`                                                      | `true/false`                     | Show or hide the collapse right arrow                                     | `true`  |
+| **arrowIcon**      | `React.ReactNode`                                              | -                                | Collapse arrow icon                                                       | -       |
+| **contentLeft**    | `React.ReactNode`                                              | -                                | Left content for the collapse                                             | -       |
+| **borderWeight**   | `NormalWeights`                                                | [NormalWeights](#normal-weights) | Border weight for `bordered` collapse                                     | `light` |
+| **index**          | `number`                                                       | -                                | Collapse's index, this value it's autogenerated by default                | -       |
+| **preventDefault** | `boolean`                                                      | `true/false`                     | Prevent default event on when is selected through the `Space, Enter` keys | `true`  |
+| **onChange**       | `(e: React.ChangeEvent, index:number, value: boolean) => void` | -                                | Callback fired when the value is changed                                  | -       |
+| ...                | `HTMLAttributes`                                               | `'id', 'className', ...`         | Div native props                                                          | -       |
 
 <Spacer y={2} />
 \n- apps/docs/content/docs/components/input.mdx (modified, 4 changes)\n  Patch: @@ -105,7 +105,7 @@ import { Input } from '@nextui-org/react';
 
 <Playground
   title="Bordered"
-  desc="You can change the full style towards a bodered `Input` with the `bordered` property and 
+  desc="You can change the full style to a bordered `Input` with the `bordered` property and 
   you can customize the color with the `color` prop."
   code={`
 <Grid.Container gap={4}>
@@ -133,7 +133,7 @@ import { Input } from '@nextui-org/react';
 
 <Playground
   title="Underlined"
-  desc="You can change the full style towards an undelined `Input` like the material effect just adding 
+  desc="You can change the full style to an undelined `Input` like the material effect just adding 
  the `underlined` prop."
   code={`
 <Grid.Container gap={4}>\n- apps/docs/content/docs/components/loading.mdx (modified, 20 changes)\n  Patch: @@ -34,7 +34,7 @@ import { Loading } from '@nextui-org/react';
 
 <Playground
   title="Sizes"
-  desc="Change the size of the entire `Loading` with the `size` property, you can also set a custom size in px."
+  desc="Change the size of the entire `Loading` with the `size` property."
   code={`
 <>
   <Loading size="xs"/>
@@ -50,6 +50,8 @@ import { Loading } from '@nextui-org/react';
 `}
 />
 
+> Note: If you want to use a custom size please see this [Codesanbox](https://codesandbox.io/s/great-montalcini-cyj39?file=/src/App.js)
+
 <Playground
   title="Types"
   desc="You can change the type of loading with the `type` prop."
@@ -130,14 +132,14 @@ import { Loading } from '@nextui-org/react';
 
 #### Loading Props
 
-| Attribute              | Type                    | Accepted values                  | Description                                | Default   |
-| ---------------------- | ----------------------- | -------------------------------- | ------------------------------------------ | --------- |
-| **size**               | `NormalSizes` `number`  | [NormalSizes](#normal-sizes)     | Loading size                               | `medium`  |
-| **color**              | `SimpleColors` `string` | [SimpleColors](#simple-colors)   | Change loading color                       | `primary` |
-| **textColor**          | `SimpleColors`          | [SimpleColors](#simple-colors)   | Change loading text color                  | `default` |
-| **type**               | `NormalLoaders`         | [NormalLoaders](#normal-loaders) | Selected or not (in single)                | `default` |
-| **gradientBackground** | `string/null`           | -                                | Set a background for gradient loading type | -         |
-| ...                    | `HTMLAttributes`        | `'id', 'className', ...`         | Native props                               | -         |
+| Attribute              | Type             | Accepted values                  | Description                                | Default   |
+| ---------------------- | ---------------- | -------------------------------- | ------------------------------------------ | --------- |
+| **size**               | `NormalSizes`    | [NormalSizes](#normal-sizes)     | Loading size                               | `medium`  |
+| **color**              | `SimpleColors`   | [SimpleColors](#simple-colors)   | Change loading color                       | `primary` |
+| **textColor**          | `SimpleColors`   | [SimpleColors](#simple-colors)   | Change loading text color                  | `default` |
+| **type**               | `NormalLoaders`  | [NormalLoaders](#normal-loaders) | Selected or not (in single)                | `default` |
+| **gradientBackground** | `string/null`    | -                                | Set a background for gradient loading type | -         |
+| ...                    | `HTMLAttributes` | `'id', 'className', ...`         | Native props                               | -         |
 
 <Spacer y={2} />
 \n- apps/docs/content/docs/components/modal.mdx (modified, 2 changes)\n  Patch: @@ -564,7 +564,7 @@ return (
         open={visible}
         onClose={closeHandler}
     >
-        <Modal.Header style={{position:'absolute',zIndex:1, top: 5, right: 8}}>
+        <Modal.Header css={{ position: 'absolute', zIndex:'$1', top: 5, right: 8 }}>
             <Text color="#363449">
                 Photo by <Link color rel="noopener noreferrer" target="_blank" href="https://unsplash.com/@vvaseemxkhan?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText">Waseem Khan</Link> on <Link rel="noopener noreferrer" target="_blank" href="https://unsplash.com/s/visual/ef7937f3-0d44-43eb-b992-28050748f999?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText">Unsplash</Link>
             </Text>\n- apps/docs/content/docs/components/pagination.mdx (modified, 4 changes)\n  Patch: @@ -99,7 +99,7 @@ return(
 <Playground
   title="Bordered"
   overflow="auto"
-  desc="You can change the full style towards a bodered `Pagination` with the `bordered` property."
+  desc="You can change the full style to a bordered `Pagination` with the `bordered` property."
   code={`
 <Grid.Container xs={12} gap={2} >  
     <Grid>
@@ -287,7 +287,7 @@ return(
 | **siblings**     | `number`                 | -                                | Amount on left/right side of selected page                       | `1`       |
 | **boundaries**   | `number`                 | -                                | Amount of elements visible on left/right edges                   | `1`       |
 | **dotsJump**     | `number`                 | -                                | Number of pages that are added or subtracted on the '...' button | `5`       |
-| **bordered**     | `boolean`                | `true/false`                     | Bodered pagination                                               | `false`   |
+| **bordered**     | `boolean`                | `true/false`                     | Bordered pagination                                              | `false`   |
 | **rounded**      | `boolean`                | `true/false`                     | Rounded pagination                                               | `false`   |
 | **shadow**       | `boolean`                | `true/false`                     | Display shadow effect                                            | `false`   |
 | **animated**     | `boolean`                | `true/false`                     | Display move and scale animation                                 | `true`    |\n- apps/docs/content/docs/components/progress.mdx (modified, 2 changes)\n  Patch: @@ -165,7 +165,7 @@ import { Progress } from '@nextui-org/react';
 
 <Playground
   title="Squared"
-  desc="You can change the full style towards a squared `Progress` with the `squared` property."
+  desc="You can change the full style to a squared `Progress` with the `squared` property."
   code={`
  <Grid.Container xs={12} sm={6} gap={2}>
   <Grid>\n- apps/docs/content/docs/components/radio.mdx (modified, 2 changes)\n  Patch: @@ -193,7 +193,7 @@ return(
 
 <Playground
   title="Squared"
-  desc="You can change the full style towards a squared `Radio` with the `squared` property."
+  desc="You can change the full style to a squared `Radio` with the `squared` property."
   code={`
 <Radio.Group value="1">
     <Radio value="1" squared>Option 1</Radio>\n- apps/docs/content/docs/components/switch.mdx (modified, 24 changes)\n  Patch: @@ -24,19 +24,27 @@ import { Switch } from '@nextui-org/react';
  <>
     <Switch />
     <Spacer />
-    <Switch initialChecked />
+    <Switch checked={true} />
  </>
 `}
 />
 
+<Playground
+  title="Initial cheked"
+  desc="You can set the `Switch` to be checked by default using `initialChecked` prop"
+  code={`
+    <Switch initialChecked />
+`}
+/>
+
 <Playground
   title="Disabled"
   desc="Unusable and un-clickable `Switch`."
   code={`
  <>
     <Switch disabled/>
     <Spacer />
-    <Switch initialChecked disabled/>
+    <Switch checked={true} disabled/>
  </>
 `}
 />
@@ -46,15 +54,15 @@ import { Switch } from '@nextui-org/react';
   desc="Change the size of the entire `Switch` including `padding` and `border` with the `size` property."
   code={`
  <>
-    <Switch initialChecked size="xs" />
+    <Switch checked={true} size="xs" />
     <Spacer />
     <Switch size="sm" />
     <Spacer />
-    <Switch initialChecked size="md" />
+    <Switch checked={true} size="md" />
     <Spacer />
     <Switch size="lg" />
     <Spacer />
-    <Switch initialChecked size="xl" />
+    <Switch checked={true} size="xl" />
   </>
 `}
 />
@@ -109,7 +117,7 @@ import { Switch } from '@nextui-org/react';
 
 <Playground
   title="Squared"
-  desc="You can change the full style towards a squared `Switch` with the `squared` property."
+  desc="You can change the full style to a squared `Switch` with the `squared` property."
   code={`
  <Switch squared color="primary" checked={true}>
     Squared option
@@ -119,7 +127,7 @@ import { Switch } from '@nextui-org/react';
 
 <Playground
   title="Bordered"
-  desc="You can change the full style towards a bodered `Switch` with the `bordered` property."
+  desc="You can change the full style to a bordered `Switch` with the `bordered` property."
   code={`
  <>
     <Switch bordered size="xs" />
@@ -198,6 +206,8 @@ import { Switch } from '@nextui-org/react';
 `}
 />
 
+> Note: If you want to use the same icons you can see the following [Codesanbox](https://codesandbox.io/s/nextui-switch-icons-glgfs)
+
 <Spacer y={3} />
 
 ### APIs\n- apps/docs/content/docs/components/textarea.mdx (modified, 6 changes)\n  Patch: @@ -71,7 +71,7 @@ import { Textarea } from '@nextui-org/react';
   title="Types"
   desc="You can use the same `Input` types in `Textarea`."
   code={`
-<Grid.Container gap={2.5} style={{marginTop: '4px'}}>
+<Grid.Container gap={2.5} css={{ mt: '4px' }}>
   <Grid>
     <Textarea placeholder="Default Textarea" />
   </Grid>    
@@ -93,7 +93,7 @@ import { Textarea } from '@nextui-org/react';
   title="Status"
   desc="You can change the color of the entire `Textarea` with the property `status`."
   code={`
-<Grid.Container gap={2.5} style={{marginTop: '4px'}}>
+<Grid.Container gap={2.5} css={{ mt: '4px' }}>
   <Grid>
     <Textarea labelPlaceholder="Default" status="default" />
   </Grid>    
@@ -121,7 +121,7 @@ import { Textarea } from '@nextui-org/react';
   desc="As well as with `Input` you can set the prop `helperText` and customise its color with the `helperColor` prop in `Textarea`
   to show a helper text."
   code={`
-<Grid.Container gap={2.5} style={{marginTop: '4px'}}>
+<Grid.Container gap={2.5} css={{ mt: '4px' }}>
      <Grid>
       <Textarea
         label="Default"\n- apps/docs/content/docs/guide/getting-started.mdx (modified, 41 changes)\n  Patch: @@ -32,6 +32,8 @@ npm i @nextui-org/react
 
 For NextUI to work correctly, you need to set up the `NextUIProvider` at the root of your application.
 
+#### React
+
 Go to the root of your application and do this:
 
 ```jsx
@@ -52,7 +54,7 @@ function App({ Component }) {
 
 #### Next.js
 
-Go to `pages/_app.js` or `pages/_app.tsx` (create if it doesn't exist) and add this:
+1. Go to `pages/_app.js` or `pages/_app.tsx` (create if it doesn't exist) and add this:
 
 ```jsx
 // 1. import `NextUIProvider` component
@@ -70,6 +72,39 @@ function MyApp({ Component, pageProps }) {
 export default MyApp;
 ```
 
+2. Go to `pages/_document.js` or `pages/_document.tsx` (create if it doesn't exist) and add this:
+
+```jsx
+import Document, { Html, Head, Main, NextScript } from 'next/document';
+import { CssBaseline } from '@nextui-org/react';
+
+class MyDocument extends Document {
+  static async getInitialProps(ctx) {
+    const initialProps = await Document.getInitialProps(ctx);
+    return {
+      ...initialProps,
+      styles: <>{initialProps.styles}</>
+    };
+  }
+
+  render() {
+    return (
+      <Html lang="en">
+        <Head>{CssBaseline.flush()}</Head>
+        <body>
+          <Main />
+          <NextScript />
+        </body>
+      </Html>
+    );
+  }
+}
+
+export default MyDocument;
+```
+
+> For more information about Next.js + NextUI SSR check out the [documentation](/docs/guide/nextui-plus-nextjs).
+
 <Spacer y={2} />
 
 ### Using NextUI components
@@ -100,7 +135,7 @@ const Component = () => <Button>Click me</Button>;
 
 | Attribute           | Type           | Accepted values              | Description                                                    | Default |
 | ------------------- | -------------- | ---------------------------- | -------------------------------------------------------------- | ------- |
-| **theme**           | `NextUIThemes` | [CreateTheme](#create-theme) | Optional custom theme, by default NextUI applies `light` theme | -       |
+| **theme**           | `NextUIThemes` | [ThemeObject](#theme-object) | Optional custom theme, by default NextUI applies `light` theme | -       |
 | **disableBaseline** | `boolean`      | `true/false`                 | NextUI automatically includes `<CssBaseline/>`                 | `false` |
 
 <Spacer y={2} />
@@ -109,7 +144,7 @@ const Component = () => <Button>Click me</Button>;
 
 <Spacer y={1} />
 
-#### Create Theme
+#### Theme object
 
 > For more information you can see the [Stitches theme documention](https://stitches.dev/docs/api#theme)
 \n- apps/docs/content/docs/guide/nextui-plus-nextjs.mdx (modified, 4 changes)\n  Patch: @@ -41,6 +41,8 @@ the post from the Next.js team</Link> to learn more.
 
 <Spacer y={1} />
 
+### Next.js
+
 In <Link target="_blank" color rel="nofollow" href="https://nextjs.org">Next.js</Link> framework, you need customization file `_document.js`,
 please refer to <Link target="_blank" href="https://nextjs.org/docs/advanced-features/custom-document" rel="nofollow">Next.js document here</Link>
 to create file `_document.js`.
@@ -72,6 +74,8 @@ class MyDocument extends Document {
     );
   }
 }
+
+export default MyDocument;
 ```
 
 Here's an example of what your `_document.js` file should look like: <Link target="_blank" href="https://github.com/nextui-org/nextui/blob/main/examples/create-next-app/pages/_document.js" rel="nofollow" color>\_document.jsx</Link>.\n- apps/docs/content/docs/layout/container.mdx (modified, 118 changes)\n  Patch: @@ -21,13 +21,13 @@ import { Container, Row, Col } from '@nextui-org/react';
   desc="Container which sets a `max-width` and a default `gap` at each responsive breakpoint"
   code={`
 <Container>
-<ExampleBlock>
-  <Row justify="center" align="center">
-    <Text h6 size={15} color="white" style={{ margin: 0 }}>
-      NextUI gives you the best developer experience with all the features you need for building beautiful and modern websites and applications.
-    </Text>
-  </Row>
-</ExampleBlock>
+  <Card color="primary">
+    <Row justify="center" align="center">
+      <Text h6 size={15} color="white" css={{ m: 0 }}>
+        NextUI gives you the best developer experience with all the features you need for building beautiful and modern websites and applications.
+      </Text>
+    </Row>
+  </Card>
 </Container>
 `}
 />
@@ -37,13 +37,13 @@ import { Container, Row, Col } from '@nextui-org/react';
   desc="Container which its `width: 100%` at all breakpoints"
   code={`
 <Container fluid>
-<ExampleBlock>
-  <Row justify="center" align="center">
-    <Text h6 size={15} color="white" style={{ margin: 0 }}>
-      NextUI gives you the best developer experience with all the features you need for building beautiful and modern websites and applications.
-    </Text>
-  </Row>
-</ExampleBlock>
+  <Card color="primary">
+    <Row justify="center" align="center">
+      <Text h6 size={15} color="white" css={{ m: 0 }}>
+        NextUI gives you the best developer experience with all the features you need for building beautiful and modern websites and applications.
+      </Text>
+    </Row>
+  </Card>
 </Container>
 `}
 />
@@ -53,14 +53,14 @@ import { Container, Row, Col } from '@nextui-org/react';
   desc="Setting it to a breakpoint `(xs, sm, md, lg, xl)` will set the `Container` as `fluid` until the specified breakpoint."
   code={`
 <Container xl>
-  <ExampleBlock>
+  <Card color="primary">
     <Row justify="center" align="center">
-        <Text h6 size={15} color="white" style={{ margin: 0 }}>
-        NextUI gives you the best developer experience with all the features you
-        need for building beautiful and modern websites and applications.
+        <Text h6 size={15} color="white" css={{ m: 0 }}>
+          NextUI gives you the best developer experience with all the features you
+          need for building beautiful and modern websites and applications.
         </Text>
     </Row>
-  </ExampleBlock>
+  </Card>
 </Container>
 `}
 />
@@ -84,46 +84,46 @@ import { Container, Row, Col } from '@nextui-org/react';
   desc="When no column widths are specified the `Col` component will render equal width columns."
   code={`
 <Container gap={0}>
-    <Row gap={1}>
-        <Col>
-            <ExampleBlock>
-                <Text h6 size={15} color="white" style={{ margin: 0 }}>
-                    1 of 2
-                </Text>
-            </ExampleBlock>
-        </Col>
-        <Col>
-            <ExampleBlock>
-                <Text h6 size={15} color="white" style={{ margin: 0 }}>
-                    2 of 2
-                </Text>
-            </ExampleBlock>
-        </Col>
-    </Row>
-    <Spacer y={1}/>
-    <Row gap={1}>
-        <Col>
-            <ExampleBlock>
-                <Text h6 size={15} color="white" style={{ margin: 0 }}>
-                    1 of 3
-                </Text>
-            </ExampleBlock>
-        </Col>
-        <Col>
-            <ExampleBlock>
-                <Text h6 size={15} color="white" style={{ margin: 0 }}>
-                    2 of 3
-                </Text>
-            </ExampleBlock>
-        </Col>
-        <Col>
-            <ExampleBlock>
-                <Text h6 size={15} color="white" style={{ margin: 0 }}>
-                    3 of 3
-                </Text>
-            </ExampleBlock>
-        </Col>
-    </Row>
+  <Row gap={1}>
+    <Col>
+        <Card color="primary">
+          <Text h6 size={15} color="white" css={{ m: 0 }}>
+            1 of 2
+          </Text>
+        </Card>
+    </Col>
+    <Col>
+        <Card color="primary">
+          <Text h6 size={15} color="white" css={{ m: 0 }}>
+            2 of 2
+          </Text>
+        </Card>
+    </Col>
+  </Row>
+  <Spacer y={1}/>
+  <Row gap={1}>
+    <Col>
+        <Card color="primary">
+          <Text h6 size={15} color="white" css={{ m: 0 }}>
+            1 of 3
+          </Text>
+        </Card>
+    </Col>
+    <Col>
+        <Card color="primary">
+          <Text h6 size={15} color="white" css={{ m: 0 }}>
+            2 of 3
+          </Text>
+        </Card>
+    </Col>
+    <Col>
+        <Card color="primary">
+          <Text h6 size={15} color="white" css={{ m: 0 }}>
+            3 of 3
+          </Text>
+        </Card>
+    </Col>
+  </Row>
 </Container>
   `}
 />\n- apps/docs/content/docs/layout/grid.mdx (modified, 52 changes)\n  Patch: @@ -23,11 +23,11 @@ import { Grid } from '@nextui-org/react';
 () => {
   const MockItem = ({text}) => {
     return (
-        <ExampleBlock height={100}>
-            <Text h6 size={15} color="white" style={{ margin: 0 }}>
-                {text}
-            </Text>
-        </ExampleBlock>
+      <Card color="primary" css={{ h: '$24' }}>
+        <Text h6 size={15} color="white" css={{ mt: 0 }}>
+          {text}
+        </Text>
+      </Card>
     );
 }
 return (
@@ -52,11 +52,11 @@ return (
 () => {
  const MockItem = ({text}) => {
     return (
-        <ExampleBlock height={60}>
-            <Text h6 size={15} color="white" style={{ margin: 0 }}>
-                {text}
-            </Text>
-        </ExampleBlock>
+      <Card color="primary" css={{ h: '$20' }}>
+        <Text h6 size={15} color="white" css={{ m: 0 }}>
+         {text}
+        </Text>
+      </Card>
     );
 }
   return (
@@ -88,11 +88,11 @@ return (
  
  const MockItem = ({text}) => {
     return (
-        <ExampleBlock height={60}>
-            <Text h6 size={15} color="white" style={{ margin: 0 }}>
-                {text}
-            </Text>
-        </ExampleBlock>
+      <Card color="primary" css={{ h: '$20' }}>
+        <Text h6 size={15} color="white" css={{ m: 0 }}>
+         {text}
+        </Text>
+      </Card>
     );
 }
   return (
@@ -113,13 +113,13 @@ return (
   desc="Auto fill remaining width."
   code={`
 () => {
-  const MockItem = ({text}) => {
+ const MockItem = ({text}) => {
     return (
-        <ExampleBlock height={60}>
-            <Text h6 size={15} color="white" style={{ margin: 0 }}>
-                {text}
-            </Text>
-        </ExampleBlock>
+      <Card color="primary" css={{ h: '$20' }}>
+        <Text h6 size={15} color="white" css={{ m: 0 }}>
+         {text}
+        </Text>
+      </Card>
     );
 }
   return (
@@ -149,11 +149,11 @@ return (
  
  const MockItem = ({text}) => {
     return (
-        <ExampleBlock height={60}>
-            <Text h6 size={15} color="white" style={{ margin: 0 }}>
-                {text}
-            </Text>
-        </ExampleBlock>
+      <Card color="primary" css={{ h: '$20' }}>
+        <Text h6 size={15} color="white" css={{ m: 0 }}>
+         {text}
+        </Text>
+      </Card>
     );
 }
 return (<Grid.Container gap={2} justify="center">\n- apps/docs/content/docs/theme/dark-mode.mdx (modified, 14 changes)\n  Patch: @@ -44,14 +44,14 @@ import { ThemeProvider as NextThemesProvider } from 'next-themes';
 const lightTheme = createTheme({
   type: 'light',
   theme: {
-    colors: {...},
+    colors: {...}, // optional
   }
 })
 
 const darkTheme = createTheme({
   type: 'dark',
   theme: {
-    colors: {...},
+    colors: {...}, // optional
   }
 })
 
@@ -84,7 +84,7 @@ const App = () => {
     <div>
       The current theme is: {type}
       <Switch
-        initialChecked={isDark}
+        checked={isDark}
         onChange={(e) => setTheme(e.target.checked ? 'dark' : 'light')}
       />
     </div>
@@ -97,7 +97,9 @@ And that's it! 🎉
 
 You have a `no-flashing` dark theme implementation for your Next.js app.
 
-> For more information about `next-themes` check out the [docs](https://github.com/pacocoursey/next-themes)
+> For more information on the `next-themes` check out the [docs](https://github.com/pacocoursey/next-themes)
+
+> For more information on the default `colors`, `fonts`, `spaces`, etc.. please refer to the [default theme documentation](/docs/theme/default-theme).
 
 ### Using use-dark-mode
 
@@ -152,7 +154,7 @@ const App = () => {
     <div>
       The current theme is: {type}
       <Switch
-        initialChecked={darkMode.value}
+        checked={darkMode.value}
         onChange={() => darkMode.toggle()}
       />
     </div>
@@ -242,7 +244,7 @@ const App = () => {
     <div>
       The current theme is: {type}
       <Switch
-        initialChecked={isDark}
+        checked={isDark}
         onChange={handleChange}
       />
     </div>\n- apps/docs/content/docs/theme/default-theme.mdx (modified, 63 changes)\n  Patch: @@ -17,7 +17,7 @@ url: https://nextui.org/docs/theme/default-theme
 NextUI includes a default color palette out-of-the-box that is a great starting point if you don't have your own specific branding in mind.
 
 You can access the colors through the `theme` object or using `tokens` whitin the `css` prop or `styled` components,
-see the [theme object](/docs/guide/getting-started) for more information.
+see the [theme object](/docs/guide/getting-started#theme-object) for more information.
 
 ```jsx
 import { useTheme, Text } from '@nextui-org/react';
@@ -27,15 +27,6 @@ const MyComponent = () => {
 
   return (
     <div>
-      <p
-        style={{
-          color: theme.colors.primary.value,
-          fontSize: theme.fontSizes.tiny.value,
-          padding: `${theme.space[2].value} ${theme.space[4].value}`
-        }}
-      >
-        Using color theme value
-      </p>
       <Text
         css={{
           color: '$blue800',
@@ -45,11 +36,62 @@ const MyComponent = () => {
       >
         Using tokens
       </Text>
+      <p
+        style={{
+          color: theme.colors.primary.value,
+          fontSize: theme.fontSizes.tiny.value,
+          padding: `${theme.space[2].value} ${theme.space[4].value}`
+        }}
+      >
+        Using color theme value
+      </p>
     </div>
   );
 };
 ```
 
+By default these colors can be used by the `color`, `borderColor`, `backgroundColor`, `fill`, and so on, here the complete list of preperties where you can use the color tokens [Stitches Property Mapping](https://stitches.dev/docs/tokens#property-mapping).
+
+```jsx
+export default {
+  colors: {
+    // generic colors
+    white: '#ffffff',
+    black: '#000000',
+    //semantic colors
+    blue100: '#CBECFE',
+    // ...
+    blue900: '#002074',
+    // ...
+
+    // brand colors
+    primaryLight: '$blue200', //  $blue600 on dark mode
+    primary: '$blue500',
+    primaryDark: '$blue600',
+
+    secondaryLight: '$purple200', // $purple600 on dark mode
+    secondary: '$purple500',
+    secondaryDark: '$purple600',
+
+    successLight: '$green200', // $green600 on dark mode
+    success: '$green500',
+    successDark: '$green600',
+
+    warningLight: '$yellow200', // $yellow600 on dark mode
+    warning: '$yellow500',
+    warningDark: '$yellow600',
+
+    errorLight: '$red200', // $red600 on dark mode
+    error: '$red500',
+    errorDark: '$red600'
+
+    // ... rest of colors
+  }
+};
+```
+
+<Spacer y={2} />
+
 <Playground
   title="Brand"
   desc="The default theme has a brand color palette, each color has its light and dark variants like `primaryLight` and `primaryDark`."
@@ -364,7 +406,6 @@ NextUI includes by default a set of common border weights.
 ```js
 export default {
   // default theme object
-  type: '...', // theme type
   colors: {...}, // color palette object
   fonts: {...}, // font families object
   fontSizes: {...}, // font sizes object\n- apps/docs/content/docs/theme/override-styles.mdx (modified, 8 changes)\n  Patch: @@ -30,6 +30,10 @@ Here are some options depending on your goals:
 NextUI provides the Stitches `css` prop for overriding styles easily.
 It's like the style attribute, but it supports tokens, media queries, nesting and token-aware values.
 
+```jsx
+import { css, Button } from '@nextui-org/react';
+```
+
 <Playground
   initialEditorOpen
   code={`
@@ -69,6 +73,10 @@ The `styled` function allows you to create a component including its styles and
 - `element` or `component`: a HTML element (`div`) or a React Component (`Component`)
 - `styles`: as many styleObject as you need
 
+```jsx
+import { styled, Button } from '@nextui-org/react';
+```
+
 <Playground
   initialEditorOpen
   code={`() => {\n- apps/docs/src/components/playground/editor.tsx (modified, 2 changes)\n  Patch: @@ -184,6 +184,8 @@ const Editor: React.FC<Props> = ({ initialOpen }) => {
           overflow: hidden;
           font-family: ${theme?.fonts?.mono};
           padding: ${theme?.space?.sm?.value};
+          max-height: min(60vh, 1000px);
+          overflow-y: auto;
         }
         .arrow {
           transition: all 0.2s ease;\n- apps/docs/src/components/sidebar/category.tsx (modified, 2 changes)\n  Patch: @@ -54,7 +54,7 @@ const Category: React.FC<React.PropsWithChildren<CategoryProps>> = ({
   const margin = 18;
 
   const postsHeight = useMemo(
-    () => routes.length * (isMobile ? 32 : 24) + margin * (routes.length - 1),
+    () => routes.length * (isMobile ? 32 : 26) + margin * (routes.length - 1),
     [routes, isMobile]
   );
 \n